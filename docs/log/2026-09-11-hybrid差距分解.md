# hybrid 的差距分解（S6 续轮，2026-09-11）

> 用户点名的方向：**v1 的 hybrid 靠 decord 侧实现，距理论性能仍有很大差距，
> v2 值得引入**。本文件是"入门诊断"——把差距按层拆开、每层给出实测数字与
> 归属，供后续（S5 解码器内联 + fork 侧改造）立项用。方法学同 S6：
> 交错 A/B（`bench ab`）、报告口径、金标 28/28 逐位一致。

## 1. 三层分解法

| 层 | 测什么 | 怎么测 |
|---|---|---|
| **L1 解码器能力** | 纯 `get_batch` 扫掠（无分段/无 OCR） | `tools/_probe_hybrid_gap.py`（同 ROI / gray / 批 64 / 3000 帧） |
| **L2 并行理想和** | 两路吞吐**相加**（并行分摊；不是调和平均） | `cpu_fps + nvdec_fps` |
| **L3 引擎口径** | 真实 extract 热轮墙钟 / `pipeline.decode` | `bench run` / `bench ab` |

`L3 损耗 = 1 − L3/L1(hybrid_gpu)`（引擎消费侧）；`L1 损耗 = 1 − L1/hybrid 理想`（解码器调度侧）。

## 2. L1：解码器能力（3000 帧，本机 7945HX + RTX 4060）

| 视频 | cpu | nvdec | hybrid(宿主) | **hybrid_gpu(设备)** | 理想和 | hybrid_gpu/理想 |
|---|---:|---:|---:|---:|---:|---:|
| h264 (test5) | 3573 | 983 | 3783 | **3052** | 4556 | 67% |
| hevc (test6_hevc) | 754 | 2036 | 1833 | **2116** | 2790 | 76% |
| av1 (test6) | 1112 | 1694 | 2686 | **2174** | 2806 | 77% |

（fork dll `build-081fix`；stock 0.8.2 wheel 对应 2821 / 2311 / 2187）

**三条结论**：
1. **`hybrid_gpu`（设备输出，GPU 管线实际用的 ctx）比 `hybrid`（宿主输出）慢
   19–24%**（h264 3052 vs 3783、av1 2174 vs 2686）；hevc 反向（2116 vs 1833）。
   设备输出路径的上载/池周转是**可指名的优化点**。
2. **调度侧仍差 23–33%**：hybrid_gpu 只到并行理想和的 67–77%。
3. **fork 未发布修复的实际收益**（`DECORD_LIBRARY_PATH=build-081fix` vs stock wheel）：
   h264 hybrid_gpu **+8.2%**、av1 持平、**hevc −8.4%（回退！）** →
   发布前需在 hevc 上复核（fork 的 chunk 预路由/池深改动对 hevc 是负收益）。

## 3. L3：引擎口径与"引擎吃掉多少"

| 配置（3000 帧热轮中位） | h264 | hevc | av1 |
|---|---:|---:|---:|
| nvdec | 3.107 s（966 fps） | **1.528 s（1963）** | 1.818 s（1650） |
| cpu | **0.994 s（3018）** | 4.249 s（706） | 2.898 s（1035） |
| hybrid（**出厂 dll**，改档位后） | 1.384 s（2168） | 1.577 s（1902） | 1.37–1.47 s（区间见下） |
| hybrid（fork dll + ct16） | 1.195 s（2511） | 1.68 s（1786） | — |
| L1 hybrid_gpu | 3052 | 2116 | 2174 |
| **引擎吃掉** | **18%** | **10–27%** | **0%** |

> 口径提醒：`av1-hybrid` 与 `h264-hybrid` 的单次 `bench run` 会给出 8–16% 的
> 轮间散布（分片比例/机器状态敏感），所以**只认交错 A/B 的中位与符号一致性**；
> 上表单点值仅作量级参考（av1-hybrid 在多次独立测量中落在 1.37–1.47s）。

**读法**：
- **av1 hybrid 已经贴着解码器能力跑**（2187 vs 2174，引擎 0% 损耗）——
  S6 的消费端优化（提交并批、窗口自适应、归约异步）正是把这块让开的。
- **h264/hevc 的引擎损耗 10–27%**，与"hybrid 需要 CPU 分片"正相关：
  解码的 CPU 侧与引擎自身的 Python/OCR 线程**抢同一批核**（C-08 早就点出
  这个代价，本轮第一次量化）。
- 因此 **hybrid 的剩余收益分两块**：①解码器调度侧（fork 工作，23–33%）；
  ②引擎侧 CPU 争用（10–27%，可用线程/优先级/分片策略优化）。

## 4. 今天的实测最优路由（本机口径）

| 编码 | 最优 | 次优 | 当前 `auto`（C-08：恒 NVDEC） |
|---|---|---|---|
| h264 | **cpu 0.994 s** | hybrid 1.195 s | nvdec 3.107 s（**慢 3.1×**） |
| hevc | **nvdec 1.528 s** | hybrid 1.577 s | nvdec（已最优） |
| av1 | **hybrid 1.372 s** | nvdec 1.818 s（慢 33%） | nvdec |

> `auto` 恒 NVDEC 优先是**用户刻意决策**（C-08：弱 CPU 上软解可能更慢、
> 且 CPU 解码必带争用/功耗），本轮**不改**——但上表说明：在这台机器上
> h264 走 `decode_backend="cpu"`、av1 走 `"hybrid"` 分别快 **3.1×** 与 **25%**。
> 路由策略属产品决策，留 C-08 复评。

## 5. 本轮已落地的小修

**hybrid CPU 线程档位 12 → 16**（`extractor._open_vr` hybrid 分支）：
交错 A/B 实测 h264-hybrid **−6.5%**、hevc-hybrid **−12.7%**（stock wheel；
fork dll 同向 −3.9%），av1 走自己的分支不受影响。旧档位"核数 3/8=12"来自
项目层 hybrid 时代；据此**取消按 `_decord_has_hybrid_preroute` 的版本门控**，
两分支合并为"核数//2 钳 [8,16]"。金标 28/28 逐位一致。

## 5.1 引擎侧损耗的两条**否定结果**（别再重复试）

引擎吃掉 10–27% 一度看起来像"生产者队列太浅 → 解码器被消费者卡住"
（默认 `buffer_size=128` 帧 ≈ 2 个解码批）。实测（交错 A/B，3000 帧热轮）：

| 变量 | h264-hybrid | hevc-hybrid | 判定 |
|---|---:|---:|---|
| `buffer_size` 128 → 512 | +0.51%（符号 +-+） | **+2.00%（+++）** | 无收益，且更深队列略劣 |
| `GPU_PIPELINE_STREAM` 0 → 1（预取流） | +0.71%（+++） | — | 无收益（与 C-10 一致） |

→ 引擎侧损耗**不是**队列/预取造成；剩余候选是 CPU 争用（解码的 CPU 分片与
引擎 Python/OCR 线程抢核，量级上 Python 每帧 25µs×3000 ≈ 75ms 只能解释其中
约 1/3）与 decord 侧 CPU chunk 上载路径，需要线程时间线级剖析——归入
hybrid 立项（§6）。

## 6. 给后续 hybrid 立项的清单（按性价比）

1. **`hybrid_gpu` 设备输出路径的 19–24% 损耗**（解码器侧）——影响所有
   GPU 管线 + hybrid 用户，是最大的单点。
2. **fork 修复发布前复核 hevc**（当前 fork dll 在 hevc hybrid_gpu 上比
   stock 慢 8.4%）。
3. **引擎侧 CPU 争用**：线程/优先级/分片策略（本机 10–27%）；可先用
   `HYBRID_CPU_THREADS` / 线程优先级做小规模实验。
4. **能力探测取代版本号门控**（v2 §17.4 D-2 `ctx.capabilities()`）——
   本轮实测已撞到它的坑：`DECORD_LIBRARY_PATH` 换 dll 不改包版本号，
   版本门控会给出与实际 dll 不符的判断。

## 7. 动手改 fork：先修掉挡路的死锁（fork `99b8785`）

要拆"混跑损耗"，就必须能用 `DECORD_HYBRID_FORCE_SIDE` 拿到"只用单侧"的反事实
速率——而这条诊断臂**本身就是挂的**：`force=cpu` 下 300 帧即死锁（一次残留
worker 烧掉 735s CPU 仍未退出）。fork 自带诊断给出确切状态：

```
[pop-stall] side=1 crdy=637 rdy=0 head=(side1,0,end=80041,exp=299,em=293) pend=655/7
```

队头 chunk 归 **GPU 侧**、期望 299 帧，但 force 让其余 chunk 全走软解 → 这个
chunk 永不被喂包；**637 帧已上载完成的 CPU 存货排在它后面**（严格的
pts/chunk 保序发射 ⇒ 队头阻塞）；force-close 安全网因 `side_pending_[GPU]=7≠0`
而正确地不敢关它。根因是两处绕过了 `ChooseSide()` 里的覆盖：

1. **Push 的 bootstrap（主因）**：首包无条件 `cur_side_ = SIDE_GPU`（NVDEC
   平价起步的设计），压根不查覆盖 → chunk0 恒归 GPU；
2. **`BuildPlan` 预路由**：按贪心份额自算 side，同样不查覆盖（本例中规划器
   根本没跑——`[hybrid-plan]` 一行未出现——但同一类不一致，一并收口）。

修复：抽出 `ForcedSide()`（未设时返回哨兵 `Side(-1)`），bootstrap、
`BuildPlan`、`ChooseSide` 三处共用。**未设 FORCE_SIDE 时逐字不改行为**（两处
都只是哨兵比较）：非强制 2000 帧 mixed 复测 2833/3077/3041（修复前
2980/2994/3071，落在本机 ±7.7% 漂移带内），且**引擎金标换本 dll 后 28/28
逐位一致**（矩阵含 hybrid 用例）。

### 7.1 修好后立刻拿到的事实：mixed 低于最优单侧

600 帧、每臂独立子进程、两遍取最优（短窗含暖机，只看相对量）：

| 编码 | 纯 NVDEC 臂 | 纯 CPU 臂 | **mixed** | 两臂理想和 | mixed vs 最优单侧 |
|---|---:|---:|---:|---:|---|
| h264 | 920 | **2200** | 1588 | 3120 | **−28%（比纯 CPU 还低）** |
| hevc | **1976** | 898 | 1334 | 2874 | **−32%（比纯 NVDEC 还低）** |

→ **当前"混跑"相对最优单侧是净亏损**（⚠️ 2026-09-12 更正：本表是 EWMA
估计器口径下的产物，"净亏损"很大程度为估计器伪影——见 §8.1 与
`hybrid_cpu_rate_ratchet`，hevc 在诚实速率下混跑**超**最优单侧）。这与 §2/§3 的"hybrid_gpu 只达理想
67–77%"是**两笔不同的账**：前者是混跑本身的调度损耗，后者是调度器距并行理想
的差距。源码注释（`ChooseSide` 顶部，2026-09-07 用户拍板）早已自陈"混跑时
软解与消费/喂包线程抢核致有效供给缩水 + 块交替发射固有损耗……后续优化方向
是压低混跑损耗而非代用户跳过"——本轮把这句话第一次变成有反事实数据的结论。

### 7.2 下一步（按此结论排序）

1. **队头阻塞（首要嫌疑）**：发射严格按 chunk 保序，而 chunk 边界 = 关键帧
   （本机 GOP 实测 ~299 帧 → 一次请求只有个位数 chunk）→ 慢臂的一个 chunk
   卡住快臂已完成的全部存货（`crdy=637` 的巨额积压就是它的直接显形）。方向：
   把**分配粒度**与 GOP 解耦（IDR-like 流允许更细边界），或收缩慢臂队头
   chunk 的期望帧数。
2. **H2D 批次被 `cpu-empty` 打碎**（`UploadStep` 的 `bufs[]` 是栈上局部量，
   队列一空就整条流 sync；观察值 3000 帧 764 次 flush / ~2340 CPU 帧 ≈ 每批
   3.1 帧，设计批 8）。**诚实标注：这条本轮只做了观察、归属未成立**——
   `no-buf=0` 与之相悖，且计数取自开 `DECORD_HYBRID_DEBUG` 的运行（13 万行
   stderr，墙钟 2940 vs 3100 fps，已被打印严重扰动）。重采方式：把计数器
   改成不带打印的 stats getter，再与 `kUploadBatch` 单变量 A/B 对照。
3. **不要动水填充判据**：`ac*rg <= ag*rc` ⇔ `ac/rc <= ag/rg`，即按**时间**
   而非帧数配平，本身是对的；要改的是粒度与发射顺序。

## 8. §7.2-1 落地：估计器才是主账（2026-09-12，fork `75b8602`）

非打印 stats 层（`DECORD_HYBRID_STATS`，热路径零 stderr，析构一次性输出）
+ 三臂矩阵（decode-only 3000f，`tools/_probe_hol_stats.py`）把 §7.2 的
"首要嫌疑"判决翻案了：

### 8.1 队头阻塞不是主因，CPU 产能估计器才是

- 实测 hevc-hybrid（EWMA 计划）：plan rc=2217-3043（真值 ≈754，虚高 3-4x）
  → CPU 臂被分 48-57% → 墙钟被慢臂实时供给钉死；`DECORD_CPU_RATE_DEBUG`
  折率轨迹给出机制：filter 每次从背压封堵脱封，头 1-3 折读到的是**解码器
  预跑存货的排空速度**（r=2491/5390/5981/9138 孤峰），"快升 1.3x/慢降 5%"
  的容量 EWMA 逐峰上抬永久卡 1200-2500 —— §6.2 时代"EWMA 防混跑相位污染"
  的设计选择，把**排空伪影当成了产能**。
- HOL 计数器（时长/episode/对侧搁置峰值）显示真实队头损失只有 7-25%，
  且大头是错分配的果（GPU 早完成干等：strand_c 实测 1000-1800 帧）。
- 修正口径（`DECORD_CPU_RATE_SUSTAINED=1`，滑窗 8 折内"连续 4 折最小值"
  的最大值 + 0.997/折慢降锁）：排除排空孤峰（撑不满 4 折）、保留 h264
  真实突发产能（连续 20+ 折；纯中位数会被 h264 饿供相位骗到 rc=995，
  实测否决）。结果：**hevc hybrid 2688-2778 > 纯 nvdec 2036**，达并行
  理想 2790 的 96-99%；av1 持平；h264 −13%（3167 vs 3537，EWMA 的高估
  恰好把 h264 推回"几乎全 CPU"的正确答案——其突发段内含 feed 低谷使
  sustained 只能读 ~2500）。**§7.1 的"混跑净亏损"很大程度是伪影**，
  保留该表的只有 h264 行与 GPU-out 路径；结论已改写进
  `hybrid_cpu_rate_ratchet`。

### 8.2 新发现：hybrid_gpu 倾斜计划死锁（sustained 暂缓默认的原因）

GPU 驻留（引擎 GPU 管线口径）+ sustained 计划在 hevc 上 ~2/8 概率挂死
（EWMA 对照 8/8 慢消费者 + 28/28 全矩阵干净）：

- 签名一：`[pop-stall] head=(CPU chunk, em=124/240) crdy=0 rdy=673
  pend=1080/3394`——CPU 子解码器携千余帧路由中量**停摆产出 0**；
- 签名二：`head=(side1, em=235/240) rdy=0 q=0 pend=538/6`——队头尾 5 帧
  滞留 NVDEC reorder，等待"进不来"的后续包解算。
- 预算/窗口核算排除 `NeedsPackets` 拦包（window≈7470≫在途），方向指向
  CPU 输出缓冲（VideoReader `ndarray_pool_`）在倾斜计划下的回收停摆。
- 份额倾斜只是改变撞窗概率——这是**先于本轮改动存在**的 bug，99b8785
  修的是它的 FORCE_SIDE 变体。复现配方（无需引擎）：慢消费者（get_batch
  + asnumpy 保留）× hevc 全片 × hybrid_gpu × sustained=1。
- 决定：sustained 保持 opt-in；默认口径 = 发布行为（金标 28/28 复验通过，
  本轮实测中 `DECORD_HYBRID_STATS` 层对默认路径零扰动）。

### 8.3 §7.2-2 的账第一次不带打印成立

hybrid_gpu hevc 非打印实测：avg_batch=**1.1**（设计 8）、nobuf=0、
cpuempty≈18k/3000 帧——批次被 `cpu-empty` 打成帧级同步，归因成立；
`kUploadBatch` 单变量 A/B 排入死锁修复之后。

### 8.4 下一步（替换 §7.2）

1. **修 hybrid_gpu 倾斜计划死锁**（签名一优先：CPU 解码器停摆）；
2. 死锁修好后 sustained 转默认 + 引擎侧 e2e 复测（hevc 预期 hybrid 显著转正）；
3. `kUploadBatch` A/B（账已立，见 8.3）；
4. h264 的 sustained 低位（feed 低谷在突发段内）：若要转正 h264，
   需要"分相位"的产能口径（如给 CPU 臂首 chunk 连续喂足再测）——收益
   排序靠后（引擎 h264 走显式 cpu）。


## 9. §8.4-1/2 落地：kick 突发治死锁 + sustained 转默认（2026-09-12，fork `4ccf889`）

### 9.1 死锁根因（签名二实证 + 修复）

kick 只送一个 IDR **冲不开 open-GOP 的 DPB**：IDR 与其 leading pictures 要等
IDR **之后**的包驱动输出。挂死现场 DPB 扣 6 帧 = IDR+5 leading，
`side_pending[GPU]=6` 吻合。旧交替计划下一 GPU chunk 的真包最迟一个 chunk 就
到，DPB 滞留是瞬态；GPU 主导的倾斜计划里 CPU 采样段连跑 3-4 chunk，离场侧
等不来任何后续包 → 瞬态变永久冻结 → 队头缺帧、盲窗被对侧库存吃满、路由停
摆、决策饥饿——环环相扣成死锁（§8.2 两签名都是它的显形）。

**修复 = KICK_BURST**：跨侧切换后把新 chunk 的前 5 个包也克隆喂给离场侧
（其输出走越界 stash / 陈旧丢弃既有兜底，帧记账与 kick 同型）。
`DECORD_HYBRID_KICK_BURST` 可消融（0 = 退回单包 kick）。慢消费者压测
（`_probe_stress_harness` 规格）**24/24 干净**（修复前 ~2/8 挂）。

### 9.2 sustained 转默认的测量链（双轨期结束）

- **gpu-out 回退未复现**：放开门控后，慢消费者配方 sustained 12/12 干净且
  墙钟与 EWMA 持平；§8.2 的 785fps 崩盘/挂死是 kick 修复前的死锁毒化，
  修复后不存在。上传批次碎裂（avg_batch 1.1-3 vs 设计 8，nobuf=0）仍在，
  但不构成回退——`kUploadBatch` A/B 降级为纯优化项（§8.4-3 保留）。
- **引擎 GPU 管线全片 e2e（配对 3 遍，段数逐位一致，
  `tools/_probe_e2e_mode.py`）**：hevc 12.73→9.66s（**−24%，3/3**）；
  **vs 纯 NVDEC 12.44s（−23%，2/2）——hevc hybrid 首次显著转正**
  （§6 时代口径"≈ 最优单侧 ±3-5%"就此改写）；h264 3.30→3.15s
  （−4.7%，3/3，配对全胜）；av1 持平（±1% 漂移内）。
- decode-only 3000f：hevc hybrid 2498 / hybrid_gpu 2659 fps（并行理想 2790
  的 90-95%）；h264 sustained decode-only −3% 但引擎 e2e 反而 −4.7%
  （sustained rc=2484 仍给 h264 分 66% CPU，份额比 EWMA 的 73% 更合理）。
- **金标 28/28 逐位一致**（fork dll，sustained 默认，含全部 hybrid 用例）。

### 9.3 代码收敛

fork `4ccf889`：`DECORD_CPU_RATE_SUSTAINED` env、`ProductionRateSustained()`、
`prod_rate_sust_` 双轨全部删除——`ProductionRate()` 即滑窗持续产能（唯一口
径），cpu-out/gpu-out 统一采用。遗留旋钮只剩消融用的
`DECORD_HYBRID_KICK_BURST`。引擎侧记录见 `hybrid_sustained_default`
（benchmarks.yaml）与 DEPENDENCIES decord 节。

### 9.4 剩余清单（接替 §8.4）

1. `kUploadBatch` A/B（§8.3 账已立，纯优化项）；
2. h264 分相位产能口径（§8.4-4 原样，收益排序最后）；
3. 0.8.3 wheel 发布（fork 八提交打包，发布前 gold + 压测再过一遍）。

## 10. §9.4-1 关账：kUploadBatch 负面结果 + kick 承重判别更正（2026-09-12，fork `281a738`）

### 10.1 上传批大小不是杠杆（§8.3 的账关掉了）

fork 281a738 把批大小做成运行期旋钮（`DECORD_HYBRID_UPLOAD_BATCH`，1=逐帧
同步旧行为），单变量四档消融：

- **上限不是约束**：8→32→64，hevc 有效批均恒 1.1——上传者消费快于 CPU 解码
  者补充，队列近恒空，cpu-empty 提前冲刷才是批均 1.1 的成因；
- **逐帧同步也无害**：batch=1 引擎全片 e2e hevc 配对 3 遍中位 9.54 vs 9.66s
  （符号混乱=漂移内），av1 9.55s、h264 3.28s 均在基线带内。

上传同步成本完全不在关键路径，§8.3"avg_batch=1.1 vs 设计 8"的账面差距
**无性能后果**——"上传链重构"正式不做，cpu-empty 提前冲刷（低发射延迟）
是正确默认。

### 10.2 kick 承重性判别（更正 §8.2 与 §9.1 的证据记载）

`DECORD_HYBRID_KICK_BURST=0` 消融：**引擎 GPU 管线全片** hevc 4/4 挂死
（sustained 默认计划），=5 同口径数十次运行零挂死 → 修复确凿承重。

⚠️ 但纯 VideoReader 慢消费者配方（3000 帧窗口与全片都试了）kick=0 也
16/16 + 4/4 干净——**§8.2"复现配方（无需引擎）"记载有误**：该死锁的判别
必须走引擎。§9.1 引用的"24/24 压测干净"（harness 3000f）对死锁无判别力，
真正证据是引擎口径的 4/4 vs 零挂死对照。

### 10.3 剩余清单（接替 §9.4）

1. h264 分相位产能口径（收益排序最后，原样保留）；
2. 0.8.3 wheel 发布（fork 九提交打包；发布前用**引擎口径**复验金标与
   kick=0 判别，harness 压测仅作冒烟）。

## 11. 全片 vs 3000f 窗口：HOL 消耗能否被时长摊薄（2026-09-12，fork dll `281a738`）

同一探针（`_probe_hol_stats.py --frames 99999`）把 decode-only 从 3000 帧窗口
放开到全片（h264 7761 / hevc、av1 23970 帧），双 ctx × 2 reps。理想和 = 3000f
单臂墙钟速率相加（4556 / 2790 / 2806）。

| case | 3000f→全片 fps | Δ | 达成率(窗→全片) | HOL% (窗→全片) |
|---|---|---|---|---|
| h264 hybrid | 3135→3975 | **+26.8%** | 69%→**87%** | 59.9→45.9% |
| h264 hybrid_gpu | 3069→3110 | +1.3% | 67%→68%（不摊薄） | 49.5→68.0% |
| hevc hybrid | 2498→2897 | +16.0% | 90%→**104%**¹ | 10.4→32.7% |
| hevc hybrid_gpu | 2659→2866 | +7.8% | 95%→**103%**¹ | 27.7→63.1% |
| av1 hybrid | 2445→2670 | +9.2% | 87%→**95%** | 7.5→32.4% |
| av1 hybrid_gpu | 2462→2304 | **−6.4%** | 88%→**82%（回退）** | 16.0→64.2% |

¹ 全片超过 3000f 口径的"理想和"分母——单臂速率本身是短窗口径，长跑更快的
臂（尤其 CPU 长连续段）被低估，非超并行理想。

### 11.1 三个答案

1. **能摊薄，但只摊薄宿主路径**：hybrid(宿主) 三码全片 +9~27%，h264 达成率
   69%→87%；瞬态成本（盲窗/估计器学习期/kick）确被时长稀释。
2. **HOL 秒数不摊薄反而增长**（g-head 绝对值 0.4-0.6s → 4.3-6.5s，strandmax
   冲到 1.7 万帧 = 快臂几乎整片跑在慢臂前面），但吞吐同向上升——HOL 时间
   本质是"快臂份额已跑完、按保序等慢臂供给"的固有等待，**HOL% 不是跨时长
   的健康指标**，跨时长看吞吐达成率才对。
3. **例外 = 设备路径（hybrid_gpu，引擎实际用的 ctx）**：h264 全片零摊薄
   （68%）、**av1 全片回退 6.4%**。av1 机制指向 sustained 产能读数长片失稳：
   rc 塌到 702（单臂真值 1112 的 63%）→ 计划 78% 倾斜 GPU → g-head strand
   17884。引擎级未显形（引擎消费节奏慢，臂间自然配平：av1 e2e sustained 与
   EWMA 持平），但 decode-only / 快消费场景（批量互补、并发流水）会踩到。

### 11.2 立项候选（接替 §10.3 的排序）

1. **av1 hybrid_gpu 长片回退**：sustained 读数对 av1 加下限钳制或分相位采样
   （与 §8.4-4 h264 分相位同型，可一并做）；
2. h264 分相位产能口径（原 §8.4-4，h264 hybrid_gpu 零摊薄是同源症状）；
3. 0.8.3 wheel 发布（口径照旧：引擎全片复验金标 + kick=0 判别）。

## 12. 控制变量重测：test6 转 h264，长片单臂 vs 混合（2026-09-12）

**动机**：§11 的达成率分母来自"不同视频 × 3000f 窗口"的单臂数字——编码、
内容、片长三个干扰项混在一起（hevc/av1 全片"超 100% 理想"即是分母失真的
信号）。把最长的 test6（av1，23970 帧）转码为 h264（`test6_h264.mp4`，
libx264 crf19 medium、High profile、**严格 GOP 300** `-sc_threshold 0`、
同 1080p60 同 ROI），同内容同长度下量四 ctx，单臂全片另行补测。

### 12.1 全片单臂（23970 帧，2 reps 中位）——分母修正

| 单臂 | 3000f 旧 | 全片新 | Δ |
|---|---:|---:|---|
| hevc cpu | 754 | **964** | +28% |
| av1 cpu | 1112 | **1290** | +16% |
| h264lg cpu | —（test5 内容 3573） | **2690** | 内容差异 -25% |
| hevc nvdec | 2036 | 2113 | +4% |
| av1 nvdec | 1694 | 1734 | +2% |
| h264lg nvdec | —（test5 983） | **965** | ≈0（固定功能不挑内容） |

NVDEC 全片≈短窗（固定函数管线不挑内容不挑片长）；**CPU 臂长跑 +16-28%**
（长连续段喂饱 david/x264 线程池）——旧"理想和"系统性低估 5-15%。

### 12.2 同内容同长度三编码对照（修正分母后的真实格局）

| 编码 | cpu | nvdec | 理想和 | hybrid | 达成 | hybrid_gpu | 达成 | dev-host 差 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| h264lg | 2690 | 965 | 3655 | 3268 | **89%** | 2944 | **81%** | 9.9% |
| hevc | 964 | 2113 | 3077 | 2897 | **94%** | 2866 | **93%** | 1.1% |
| av1 | 1290 | 1734 | 3024 | 2670 | **88%** | 2304 | **76%** | 13.7% |

- **编码是第一干扰项**：同内容下 CPU 单臂 h264/av1/hevc = 2690/1290/964
  （2.8×）——此前"h264 最远（67%）"的账里有 25% 是分母错（test5 的 CPU
  3573 套在 test6 内容上）；修正后 h264 hybrid_gpu 实为 81%。
- **dev-host 差与 CPU 臂产能正相关**（hevc 1.1% / h264 9.9% / av1 13.7%）：
  hevc CPU 臂慢，上载链永不 bind；h264lg hybrid_gpu 的 HOL 66% 是 c-head
  （3.45s，strandmax 仅 440/571——不是保序积压而是**交付跟不上**），
  上载链交付上限在快 CPU 编码上显形（与 §10.1"batch 同步不在关键路径"
  不矛盾：那是消费 paced 场景，这里是 decode-only 快消费把上载能力打到顶）。
- **av1 hybrid_gpu 76%（三码最差）主因 = 一次性规划冻结在坏读数**：全片
  av1 hybrid_gpu 仅 6 个折样本，rc=702（真值 1290 的 54%）是首窗饿供相位
  读数；BuildPlan 一次性读、慢降锁无机会修正（§8 时代锁保的是"读数不随
  时间棘轮"，不是"规划会重读"）。

### 12.3 修订后的立项排序（接替 §11.2）

1. **av1/h264 首窗读数钳制或分相位采样**（给 CPU 臂先喂足一整个 chunk
   再采读数；修 BuildPlan 一次性冻结的输入质量）——同时修 av1 的 76% 与
   h264 的份额过倾；
2. 设备路径上载交付上限（快 CPU 编码的 c-head）——先量化上载能力天花板
   （batch=3.1 有效值 vs 理论），确认后再决定是否值得动；
3. 0.8.3 wheel 发布（口径不变）。

## 13. replan 实验：机制被否，但 av1 归因翻案了（2026-09-12）

### 13.1 做了什么

按 §12.3-1 实现"重规划"：CPU 读数每前进 ≥8 折且新能力比偏离冻结份额
≥8pp 时，在 chunk 边界用最新读数重建未来计划（已发射/在途不触碰，跨侧
切换走既有 kick+突发；`DECORD_HYBRID_REPLAN=0` 消融开关，stats 行加
replans/share 字段）。3 码 × 3 对交错全片 A/B（decode-only hybrid_gpu）。

### 13.2 结果：replan 一次都没触发（replans=0，6/6 cell）

不是 bug——av1 的 rc 在**全部 125 个折样本**上稳定在 702-875，从未偏离
首窗冻结值 ±8pp 之外。§12.2"一次性冻结在首窗坏读数"的归因**错了**：
sustained 读数是稳的、对的；"错"的是把纯臂 1290 当成了混合期应有的供给。

### 13.3 翻案后的账：CPU 臂混跑折价才是全部剩余差距

混跑期 sustained 读数（= 混跑中的真实产能）与纯臂的比值：

| 编码 | CPU 纯臂 | 混跑期 rc | 折价 | 混跑理想 rc+nvdec | 实测 | 达成 |
|---|---:|---:|---:|---:|---:|---:|
| av1 | 1290 | ~760 | **−41%** | ~2494 | 2304-2520 | **~95%** |
| h264lg | 2690 | ~2100 | −22% | ~3065 | 2799-2944 | ~94% |
| hevc | 964 | ~820 | −15% | ~2933 | 2818-2866 | ~97% |

**调度器已达自身上限（94-97%）**——剩余差距全部是"CPU 软解臂在混跑期
供给缩水"（与 NVDEC 喂包线程/消费线程/上载抢核 + 交替发射固有成本；
§2 时代注释的定性描述第一次有了分编码的定量：折价与 CPU 臂线程数/帧
耗时正相关，av1 dav1d 多线程重帧受害最深 −41%）。此前"av1 hybrid_gpu
长片回退 76%"、"h264 81%"的分母虚高已由此解释。

### 13.4 处置与排序

- replan 实现**已回滚**（fork 工作区还原，未提交——触发不了的机制是死
  代码）；但本实验的**结论必须保留**：重规划/首窗钳制/分相位采样这一族
  思路全部关闭，估计器输入没有问题。
- 立项排序更新：① CPU 臂混跑折价的供给侧（av1 最重）——方向是线程数/
  喂包隔离，不是调度器；② 设备路径上载天花板量化（§12.3-2 原样）；
  ③ 0.8.3 wheel 发布。**h264/av1 在引擎里走显式 cpu/既有路由，这些差距
  多数场景已被绕行**——优先级因此都不高，剩余工作以验证与发布为主。

## 14. §13.4-1 落地：CPU 臂线程错配伪影 + 引擎档位统一 codec 感知策略（2026-09-12）

### 14.1 口径事故：hybrid_gpu 探针一直跑 16T，单臂跑 32T

追"CPU 臂混跑折价"时先撞到一个**测量口径 bug**：`_probe_hol_stats` 的
WORKER 对 hybrid_gpu 传 `num_threads=0`，而 decord 内（video_reader.cc
`SetVideoResampleDriver`）hybrid ctx 的 0 = `hw//2 钳 [2,16]` = **本机 16T**；
单臂 cpu 格子传 32。于是 §13.3 的"av1 折价 −41%"实为 **16T 混跑 vs 32T
纯臂**的错配比较；§11.3"av1 长片回退 6.4%"、§12.2"76%"同样是 16T 伪影。
引擎实况三个口径互相不可比：av1 传 24、h264/hevc 传 16（`//2 钳 [8,16]`
旧档）、探针 16/32 混用。探针加 `--nts` 旋钮显式给 nt（勿再依赖 0 的
隐式落点）。

### 14.2 同口径线程消融（全片 23970f，2 reps 中位，轮内交错；fork dll 281a738）

| fps | 16T | 24T | 32T |
|---|---:|---:|---:|
| av1 纯臂 / hybrid / hybrid_gpu | 845 / 2500 / 2526 | 1248 / 2676 / **2782** | 1267 / 2724 / 2786 |
| h264lg 纯臂 / hybrid / hybrid_gpu | 2177 / 2982 / 2829 | 2433 / **3279** / 3035 | 2410 / 3278 / 3096 |
| hevc 纯臂 / hybrid / hybrid_gpu | 893 / 2817 / 2743 | 923 / 2810 / 2835 | 949 / 2924 / 2741 |

- decode-only：**24T 甜点**（av1 +10%、h264lg +7~10%），32T 无进一步收益；
  hevc 平（CPU 臂慢、份额小）。
- **同 nt 诚实折价**（CPU 臂混跑速率 = 总量×CPU 份额 vs 同 nt 纯臂）：
  av1 −7%（16T）/−14%（24T）、h264lg −10~−18%、hevc ≈−23%——远小于 −41%，
  且折价随 nt 升高而加深（过订阅放大）。"调度器已达 94-97% 混跑理想"的
  §13.3 主账**不受影响**（分母分子同错配相消），但折价的绝对量级改判。

### 14.3 引擎 e2e 定档（`_probe_hybrid_threads_e2e`，独立子进程交错配对 3 遍，文本集 sha 逐位一致）

| Δ(B−A)/A | 16 vs 24 | 24 vs 32 | 32 vs 48 |
|---|---:|---:|---:|
| h264lg | **−5.9%**（3/3） | **−4.8%**（3/3） | −2.4%（2/3，边际） |
| hevc | **−4.4%**（3/3） | 持平（符号 2/3 混） | — |
| av1 | **−12.8%**（3/3） | **+5.1% 回退**（2/3） | — |

**引擎口径顶点 = h264 32 / hevc 32 / av1 24**——恰为 `_decode_num_threads`
的现行 codec 感知策略（h264+TRT 取满逻辑核钳 32、hevc 32、av1 24）。
decode-only 的"24T 饱和"在引擎消费节奏（OCR/分段线程与解码共存）下不成立。
落地：`extractor._open_vr` hybrid 分支删除 `//2 钳 [8,16]` 旧档与 av1 特例，
统一调 `_decode_num_threads(codec=...)`；`HYBRID_CPU_THREADS_AUTO_MIN/MAX`
常量删除；README/knobs/测试同步。av1 行为不变（原本就走 24）。

### 14.4 金标事故与重录（预存，非本轮改动引入）

换 dll 后 `--verify` 25/28：**C-h264 三例（cpu/nvdec/hybrid 全后端）DIFF**
——nvdec 不含 CPU 线程也挂，即与线程无关。根因：**test6_h264.mp4 在 §12
被重新转码**（严格 GOP 300、`-sc_threshold 0`），sha `c948c66b…`→
`06e19392…`，而 C-h264 向量录于 09-11（旧文件）。差异模式吻合：文本
全同、rep_frame 帧级偏移（32↔34、44↔55）、置信度微移。处置：`record.py
--case C-h264` 重录三例 + `write_manifest` 刷新（人不得手改），复验 28/28。
（一次 C-h264-cpu 末位置信度 ±0.0001 抖动复跑 3/3 稳——F-7 已知敏感段
现象，纪律门禁"段数+唯一文本集"恒一致。）

### 14.5 稳定性复验（新档 32T；fork 零改动，本轮全部在引擎侧）

- 压测 harness 慢消费者全片 ×6 trials × 3 码（hevc/av1/h264lg hybrid_gpu）
  = **18/18 零挂死**，墙钟稳定（hevc 8.3-8.4s 带）；
- 引擎 hevc 全片 ×3 零挂死、段数一致（8243）；
- kick 突发承重性不变（dll 未动）；纪律审计/INDEX 审计全绿。

### 14.6 剩余清单（接替 §13.4）

1. **0.8.3 wheel 发布**（口径不变：引擎全片复验金标 + kick=0 判别；
   开发 dll md5 更新 `2fd49ea5`——replan 回滚后重建，非确定性构建产物）；
2. 设备路径上载天花板量化（§12.3-2 原样，低优先）；
3. matched-nt 折价 −7~−23% 的供给侧：优先级进一步降低——引擎档位已对齐
   cpu 后端，剩余差距在引擎口径多为消费节奏吸收。

## 15. 0.8.3 wheel 发布验证轮（2026-09-12 续，fork 零新提交）

### 15.1 本地构建与安装

`pip wheel .`（scikit-build-core 隔离构建，FFMPEG_DIR=D:/Software/ffmpeg9）
产出 `decord-0.8.3-cp313-cp313-win_amd64.whl`（64MB，dll 闭包完整：
decord.dll + avcodec-63/avformat-63/avutil-61/swscale/swresample/avfilter），
sha256 `bad116d0…`；已装进本机 python313（0.8.2 wheel 在 fork dist/ 可回滚）。
三处版本源（libinfo.py/pyproject/c_runtime_api.h）核对一致 = 0.8.3。

### 15.2 金标：28/28 ×2（连带修复一处录制口径坑）

首轮 wheel 全量 verify 27/28：`C-h264-cpu → ocr.segments`。定位（golden_diff
冷/热两跑）：结构/文本/rep_frame **逐位同**，仅 19/1108 段置信度第 4 位小数
翻转——昨天用 `--case C-h264` **局部重录把向量录在冷池态**，而全量 verify
按序跑到它时池已热（F-8 已记载的 numpy↔CUDA 预处理浮点流差异）。修复 =
整矩阵同序重录（`record.py` 不带 `--case`），复验 28/28 ×2 稳定。FINDINGS
F-8 补"录制同理"注记。

### 15.3 kick=0 判别不可复现（两构建同口径），证据链改判

§10.2 的发布门禁"kick=0 引擎全片 hevc 4/4 挂死"**今天对 wheel 与 dev dll
都不再复现**（各 4 次全干净，16T/32T 档都试了）——死锁窗口本身时序敏感，
当日机器状态不可再现。替换证据链：①默认运行 stats 显示 `kicks c=30 g=30`
（kick 冲刷在每次跨侧切换都在跑，代码活跃）；②wheel≡dev 全对位（金标 /
e2e 三码 / 压测 / decode-only 2753 vs 2782fps）。kick 承重性的历史对照
（4/4 挂死 vs 数十次零挂死）保留为 §10.2 记载。

### 15.4 验证矩阵（wheel 口径，无 DECORD_LIBRARY_PATH）

| 项 | 结果 |
|---|---|
| 金标 28 用例 | **28/28 ×2** |
| 引擎 e2e 全片×2 | h264lg 9.06/9.18s、hevc 9.97/9.64s、av1 9.62/10.43s，段数 8241/8243/8241 与 dev dll 一致 |
| 压测 harness 全片 32T | hevc/av1/h264lg hybrid_gpu **6/6 零挂死** |
| decode-only 对位 | av1 hybrid_gpu@24 = 2753fps（dev dll 2782，±1% 漂移内） |
| kick 激活 | kicks c=30 g=30（hevc e2e 全片） |

### 15.5 发布剩余步骤（对外动作，留用户执行）

fork master 领先 origin 九提交未推送；发布 = `git push origin master` 后在
GitHub Actions 手动 dispatch `release.yml`（version=0.8.3）——流水线构建成功
后自动 bump（已同版本则跳过）+ 打 tag v0.8.3 + 发 release（notes=自上一
tag 的提交列表，cp39–cp314 全版本 wheel）。本地 cp313 wheel 已验证，CI 产物
发布后可抽验（pip 安装 + 本节矩阵复跑）。

## 16. 宿主路径环死锁修复 + "全面反超"判定矩阵（2026-09-12 续，fork `02c91e6`）

### 16.1 事故：hybrid + ONNX(CPU OCR) 宿主管线挂死（2/2 必现）

跑"两种输出路径全面反超"缺臂（宿主路径 e2e）时撞出死锁：h264lg
`hybrid+cpu(ONNX)` 无 CPU 占用静止。STATS 取证签名（零打印扰动）：

```
[pop-stall] side=1 rdy=0 head=(side1,0,end=300300,exp=300,em=293) pend=7037/13
[pop-stall2] cq=3354 qpk=0 np=0 si=1 kick=0/1 infl=7050 cud=0/5/0
```

**两层致死环**（第一层修掉后第二层显形，首跑 23.2s 是侥幸）：

1. **突发递送被在途窗口截断**：kick 突发 = 切换时克隆 1 个 IDR + 后续
   Push 逐个克隆 ≤KICK_BURST 个；ONNX 慢消费让 CPU 臂积压 7037 帧把
   `inflight ≥ 窗口` 顶满 → demux 停读 → 剩余克隆永远不来（kick=0/1）。
   环：消费者等队头 → 队头等突发 → 突发等 demux → demux 等排水 →
   排水等队头。TRT 快消费下 inflight 到不了窗口顶，突发瞬间送完——
   gpu-out 全片/压测（18/18）全部测不出该窗口。
2. **KICK_BURST=5 对深重排流不够**（放行修好后签名不变才暴露）：克隆包
   已全部喂入（qpk=0），NVDEC 仍扣 5 帧（cud=0/5/0）、em=293/300 冻结
   ——libx264 medium B 金字塔的重排深度 >5，5 个输入推不满 display
   尾帧额度。`DECORD_HYBRID_KICK_BURST=16` 单变量 3/3 干净证实。

**修复（fork 02c91e6）**：①`NeedsPackets()` 对武装中的突发无条件放行
（≤16 个压缩包，有界；Push/NeedsPackets 同消费线程无竞态）；②
KICK_BURST 5→16（SPS max_dec_frame_buffering 规格上界；代价 = 每切换
离场侧多解 ~11 陈旧帧，走既有越界丢弃）。**消融对照**：只修①不改②
仍挂（rc=124）；②=16（env）+① 后 4/4 干净。

### 16.2 修复回归（dev dll + wheel 双口径）

金标 28/28；TRT e2e 三码同带（hevc 9.48 / h264lg 8.72 / av1 9.48s）；
压测 3 trials × 3 码全片 9/9 零挂死；decode-only av1@24 2614fps
（漂移带内）。wheel 重建（sha16 `1b180227`）装后：挂死用例 23.6s 干净、
TRT hevc 9.62s、金标 28/28。

### 16.3 "全面反超"判定矩阵（全片 e2e，三后端 × 两输出路径）

**设备路径（TRT，hybrid_gpu）**：三码全面反超最优单侧——

| TRT/设备 | cpu | nvdec | **hybrid_gpu** | vs 最优单侧 |
|---|---:|---:|---:|---|
| h264lg | 10.28 | 25.84 | **9.12** | **−11.8%** |
| hevc | 26.33 | 12.38 | **9.97** | **−19.5%** |
| av1 | 20.72 | 14.93 | **10.02** | **−32.8%** |

**宿主路径（ONNX，hybrid）**：**不反超**——

| ONNX/宿主 | cpu | **nvdec** | hybrid | 判定 |
|---|---:|---:|---:|---|
| h264lg | 25.3 | 25.6 | ~25.5 | 三者持平（OCR 瓶颈） |
| hevc | 34.6 | **20.9** | 23.2 | hybrid 慢 **+11%** |
| av1 | 31.1 | **20.8** | ~27.3 | hybrid 慢 **+31%** |

机制：ONNX 吃满核时 hybrid 的 CPU 解码臂与 OCR 抢核，纯 nvdec（零 CPU
解码）结构性占优；decode-only 的混跑优势在消费瓶颈下不可变现。
**结论：hybrid 的收益面 = TRT/设备路径（全面反超）；ONNX/宿主路径应选
nvdec**（引擎 auto 本就落 NVDEC，仅显式 hybrid 会踩到——文档警示即可）。

decode-only 口径（§14.2）6 格 hybrid/hybrid_gpu 仍全部反超最优单臂
（+27~60%），不受本节影响。
