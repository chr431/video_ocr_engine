# 2026-09-14 OpenVINO 移除轮：onnxruntime 从项目彻底删除（用户裁决）

> 命题（用户）：证据已足够支持删除——彻底移除 ORT；**开发环境保留
> onnxruntime 安装**（探针对照臂仍可用），仅项目代码/依赖面剥离。
> 依据 = 模型级 A/B + 真值门禁三内容族零差 + bench ab −27.96%
> （log OpenVINO模型级A-B/集成轮）；非 x86 部署风险由用户确认不存在。

## 删除面（分支 remove-ort）

| 面 | 处置 |
|---|---|
| `native.py` ORT 分支 | `_init_onnx` = OpenVINO 唯一（缺席 raise + 提示 pip install）；`_infer_locked` 删 ORT 支路；`backend_name` 只剩 tensorrt/openvino |
| `OCR_CPU_BACKEND` 旋钮三件套 | 全删（constants/knobs/run_config）——**从未随版本发布，零迁移面** |
| 测试 | backend_name 单测收敛为 openvino；env 快照 23→22、knobs 25→24 |
| 文档 | MIGRATION §2.0（onnxruntime 依赖移除，0.14.0）；DEPENDENCIES 删行；C-48 更新为"唯一引擎 + 冻结可剪 73MB" |

保留：`engine_type="onnxruntime"` 字符串 = CPU 路径历史标识（API 零变更）；
`_probe_ov_cpu_ab.py`/`_probe_quant_static.py` 探针（环境装着 ORT 仍可跑）。

## 门禁

全套 219 过；金标 verify 28/28（OV 基线本就一致）；discipline/index
审计绿；conclusions 源 12240/12288。
