# 2026-09-13 hybrid 解码率 vs 两解码器理论并联和（第一笔）

## 口径
- 三编码为**同内容族**（test6 三件套），ROI 一致（841,994,949,1026），3000 帧。
- 消费端统一 `ocr=trt`，以**隔离解码**（TRT 几乎不吃 host CPU，避免 CPU OCR 与
  CPU 解码抢核污染对照）。
- 指标取 `pipeline.decode` 的 span sum；单臂各跑一次（**非 A/B**）。
- 命令：`SURVEY_FRAMES=3000 SURVEY_CONFIGS=cpu/trt,nvdec/trt,hybrid/trt
  python tools/_probe_path_survey.py`

## 数据
| 编码 | cpu/trt decode | nvdec/trt | hybrid/trt | 理论并联和 | 并联效率 |
|---|---|---|---|---|---|
| h264 | 1.418s（2116 fps） | 3.208s（935 fps） | 1.716s（1748 fps） | 3051 fps | **57.3%** |
| hevc | 3.438s（873 fps） | 1.520s（1974 fps） | 1.598s（1877 fps） | 2847 fps | **65.9%** |
| av1 | 2.460s（1220 fps） | 1.809s（1658 fps） | 1.514s（1982 fps） | 2878 fps | **68.9%** |

（理论并联和 = 两单臂速率直接相加；并联效率 = hybrid 实测 ÷ 理论并联和。）

## 三条读法
1. **并联效率只有 57–69%**：hybrid 拿到两解码器理想和的约三分之二，调度效率
   仍有空间（与 C-39 记的"调度器达混跑理想 94–97%"口径不同，那条是**混跑份额
   兑现率**，本条是**速率相对理论并联和**，两者不可互换）。
2. **h264 上 hybrid 输给单臂 CPU**：1748 vs 2116 fps（−17%）。而 NVDEC 在 h264
   上是硬件天花板（935 fps，C-04 记 982 fps 同级）——故 h264 的"混合"实际是在
   用一条明显更慢的腿拖累一条更快的腿。这与 C-08 记的"h264 CPU 软解快 1.7–2.8×"
   以及本仓路径普查里 `cpu/trt` 优于 `nvdec/trt` 一致。
3. hevc 上 hybrid（1877）也略输纯 NVDEC（1974，−5%）；只有 av1 上 hybrid 反超
   纯 NVDEC（1982 vs 1658，+20%）。**"混合必然更快"不成立，方向随 codec 变。**

## 待办
- 本笔是单臂数据，按纪律需 `bench ab` 交错确认后才能作结论。
- 归因（为何只有 57–69%）：用 `tools/_probe_hybrid_reeval.py` 取 fork 侧
  `[hybrid-stats] busy`（每臂忙时）+ `hol`（队头阻塞）+ `plan`（份额兑现）。
  该工具需 `DECORD_FORK_BUILD` 指向含 busy 计数的构建树；本机
  `D:\Repo\decord\build-081fix`（decord.dll 重建于 2026-09-13 00:14，晚于
  fork `6da2957`）是候选。

## 修正与本轮归因（同日续）

### ⚠️ 更正一：上一节的"h264 hybrid 输给单臂 CPU −17%"被交错 A/B **否决**
交错 A/B（`h264same-nvdec`，3000 帧，ocr=trt，repeat 3）：

| 对照 | A 热池 | B 热池 | Δ% | 符号 |
|---|---|---|---|---|
| h264：cpu → hybrid | 1.6212 | **1.5842** | **−2.28%** | `---` |
| hevc：nvdec → hybrid | **1.5917** | 1.6656 | **+4.64%** | `+++` |
| av1：nvdec → hybrid | 1.8751 | **1.4726** | **−21.47%** | `---` |

**错因**：上一节拿**单臂的 `pipeline.decode` span** 做跨臂比较，但 cpu 臂的
decode 只占其墙钟 67%（1.418/2.117s）——span 对比不等于端到端对比，
"理论并联和"那套算法因此失真。

### ⚠️ 更正二：全片口径下 hybrid 在**三编码上全部优于纯 NVDEC**
`tools/_probe_hybrid_reeval.py`（`DECORD_FORK_BUILD=D:\Repo\decord\build-081fix`，
全片、两轮）：

| 编码 | nvdec | hybrid | Δ |
|---|---|---|---|
| h264（2735 段） | 8.78 / 8.71s | 3.83 / 3.87s | **−55.9%** |
| hevc（8340 段） | 12.69 / 12.78s | 10.62 / 10.96s | **−14.2%** |
| av1（8340 段） | 15.23 / 15.24s | 10.32 / 10.71s | **−29.7%** |

**与上一节 3000 帧窗口 A/B 的 hevc 结论相反**（窗口：nvdec 快 4.6%；全片：hybrid
快 14.2%）。换算速率可见端倪：窗口下 hybrid hevc ≈1801 fps，全片 ≈2187 fps
——即 **hybrid 的速率随运行长度改善**，3000 帧窗口摊销不掉启动期。

### 归因（fork 侧 `[hybrid-stats] busy`）
| 编码 | GPU 臂忙 | CPU 臂忙 | 读法 |
|---|---|---|---|
| h264 | 1621 fps / **41%** | 2681~3086 fps / 57% | 两臂都未跑满，存在协调缺口 |
| hevc | 5466 fps / **33%** | 795 fps / **93%** | **GPU 臂闲置 2/3，CPU 臂满载却只有 795 fps** = 份额过多给了慢腿 |
| av1 | 4036 fps / 38% | **0 fps** / 74~82% | CPU 臂"忙"但产出 0 fps（计数口径待查） |

### 待办
1. 解释窗口 vs 全片的差异（启动期摊销），并把口径统一到全片。
2. 优化份额分配：hevc 的 GPU 臂 33% 忙是最明确的低效点；h264 两臂皆不满。
3. 查 av1 的 CPU 臂 busy fps=0 是否是计数口径问题。

## 深挖（同日续二）：单侧基线 + 效率正确口径 + 瓶颈定位

### 决定性实验：强制单侧（`DECORD_HYBRID_FORCE_SIDE`，功能有效）
全片、min-of-2：

| 编码 | auto（混跑） | side:gpu | side:cpu | 理论并联和 | 效率 | vs 最佳单臂 |
|---|---|---|---|---|---|---|
| h264（7761 帧） | **3.461s**（2243 fps） | 8.765s（885） | 3.791s（2047） | 2.65s | **76.5%** | 1.10× |
| hevc（23970 帧） | **10.623s**（2257） | 12.799s（1873） | 29.358s（816） | 8.91s | **83.9%** | 1.20× |
| av1（23970 帧） | **10.824s**（2214） | 15.262s（1571） | 15.241s（1572）※ | ~8.6s | ~79% | 1.41× |

※ av1 非 IDR-like，`force:cpu` 按代码退化为 GPU（两行 15.26/15.24 相同即证据）；
故 av1 的 CPU 臂速率取自路径普查（1220 fps）。

### ⚠️ 更正三：并联效率的正确分母是「单侧端到端速率」，不是「忙时速率」
先前用 `busy_g=5466 fps` 当分母算出 36% 效率是**错的**——强制 `side:gpu` 显示
GPU 臂在**独占时也只忙 43.8%**、wall 12.80s ≈ 纯 NVDEC 12.69–12.78s，说明
"忙时 5441 fps" 不是可持续速率。改用单侧端到端速率作分母后：hevc 效率 **83.9%**。

### ⚠️ 更正四：`DECORD_HYBRID_FORCE_SHARE` 是**死旋钮**（实测无效）
`BuildPlan(key_pts, cpu_share)` 的分配循环只用 `rc`/`rg`：
`Side s = (ac*rg <= ag*rc) ? SIDE_CPU : SIDE_GPU;`，形参 `cpu_share` **在 fork 源码里
只出现三次**（签名 749 / 声明 .h:495 / 调试打印 799）——**从未参与分配**。
故份额扫描（0.02→0.35）扫到的是噪声：wall 10.82–11.20s（±3.5%）、CPU 包数
6341–7561 基本不动。分配实际由「速率比 + 积压反馈 + 70% 迟滞」决定，且
**规划冻结规划时速率比、运行中不做重规划（fork 内注释自陈 v1 取舍）**。

### 瓶颈定位：host 侧，不是调度器
`ENGINE_PROFILE=1` 分相（hybrid）：

| 相位 | hevc（wall 12.144s） | h264（wall 3.452s） |
|---|---|---|
| producer `decode_batch` | **11.024s（91% wall）** | **2.993s（87%）** |
| ├ `consume_feed` | 3.136s | 1.076s |
| └ `merge_pair` | **2.610s（21% wall）** | 0.939s |
| consumer `q_get_wait` | 10.565s（**87% 空等**） | 2.887s（84%） |
| `ocr.infer` | 6.887s | 2.341s |

- **生产者线程占墙钟 87–91%**，其中近半是 host 计算（`consume_feed` + `merge_pair`）。
- 消费者 84–87% 时间在空等 → 消费侧有大量余量，瓶颈在生产者链路。
- **hybrid 实测速率三码几乎相同（2214 / 2243 / 2257 fps）** —— 与"三编码解码器
  速度差异巨大"（1571–1873 vs 816 fps）形成反差，说明 ~2250 fps 处有一个
  **与编码无关的封顶**，来自 host 生产者链路而非解码器。

### 结论（本轮）
1. hybrid 在**三编码上均快于两侧单臂**（全片口径），用户目标在"更快"这一半已达成；
   "稳定"这一半未达成——3000 帧窗口下 hevc 会输（启动期摊销）。
2. 剩余效率损失（16–30%）**不在份额分配**（旋钮是死的），而在 host 生产者链路
   与慢腿队头阻塞（hevc 的 `hol_c` 1.54s）。
3. 后续优化的靶子（按量级）：hevc 的 `hol_c`、以及三码共有的 host
   `consume_feed`+`merge_pair`（占 wall 21–33%）。

## 深挖（同日续三）："稳定"边界与两处方法论更正

### 窗口曲线（全片=23970/7761 帧；min-of-2，独立子进程）
auto（hybrid）vs `side:gpu`（纯 GPU 臂），win 者为粗体：

| 编码 | w=1000 | w=3000 | w=6000 |
|---|---|---|---|
| **h264** | **auto 1.420** vs gpu 1.638（+13%） | **auto 2.235** vs 3.751（+68%） | **auto 3.240** vs 6.932（**+114%**） |
| **hevc** | auto 1.419 vs **gpu 1.090**（gpu +30%） | 2.079 vs 2.081（持平） | **auto 3.295** vs 3.614（+10%） |
| **av1** | auto 1.286 vs **gpu 1.161**（gpu +11%） | **auto 2.013** vs 2.360（+17%） | **auto 3.207** vs 4.222（+32%） |

**读法**：
- **h264 无交叉点**：hybrid 从 1000 帧起就赢，且优势随长度放大（6000 帧已 +114%，
  全片 +153%）。原因是 h264 的 NVDEC 撞硬件天花板（885 fps），CPU 臂提供的
  增量相对巨大。
- **hevc / av1 有交叉点，约在 1000–3000 帧之间**（hevc 更靠后）：短片下
  hybrid 的启动摊薄不足，纯 NVDEC 反而更快。

hevc 线性拟合（`wall = a + n/r`）：auto r=2394 fps / a≈0.99s；纯 GPU r=1949 fps /
a≈0.59s → 交点 ≈4165 帧。即 **hybrid 稳态快 23%，但多付约 0.4s 启动**。

### ⚠️ 更正五：那个"0.4s 启动"不是 GPU 校准（我的进程内实验有顺序伪影）
先在一**进程内**顺序跑 auto/side:gpu，看到 auto 的 `gpu_calib_total=0.381s` 而
side:gpu 只有 0.084s，误判为"hybrid 多付 0.29s 校准"。
实际 0.381s ≈ C-36 记的 **TRT 反序列化 0.39–0.44s**——**进程级冷启动被第一个
arm 独吞**，与路线无关。
改用**独立子进程**（工具默认方式）重测 w=1000：auto 1.419 / side:gpu 1.090 /
prefetch=8 1.360 / nopinned 1.381 → **那些池与预取旋钮均无效**，真正的差距
仍在别处（未定为单一成分）。

### 结论（至今）
1. **相对纯 NVDEC**：h264 稳赢；hevc/av1 仅在 >约 1500–3000 帧后赢。
   **相对 CPU 臂**：三码全赢（h264 1.10×、hevc 2.76×、av1 1.81×，全片口径）。
2. 因此"任何编码都**稳定**快于单臂"目前**不成立**，缺口唯一且明确：
   **hevc/av1 在短片（≤约 1–2k 帧）上输给纯 NVDEC**，代价来自启动摊薄。
3. 已知不是原因的：份额分配（`FORCE_SHARE` 是死旋钮）、池/预取旋钮（实测无效）。
4. 已知的真约束：host 生产者链路（`decode_batch` 占 wall 87–91%，其中
   `consume_feed`+`merge_pair` 占 21–33%）、hevc 的慢腿队头阻塞（`hol_c`）。

### 建议的下一步（按证据）
- **短窗口启动期**：需要 fork 侧的启动期可视化（`DECORD_HYBRID_DEBUG` 的
  `[hybrid-budget]`/`[hybrid-plan]` 行）才能定位那 0.4s 的具体成分。
- **host 链路**：`merge_pair`（hevc 占 wall 21%）跑在生产者线程，而消费者
  84–87% 时间空等 —— 把合并工作移出生产者线程是结构性收益点。

## 深挖（同日续四）：交错 A/B 确认交叉点（hevc，短片 vs 长片）

`tools/bench.py ab --config hevc-hybrid --ocr-backend tensorrt`，A=auto(hybrid) B=`side:gpu`
（`--env-b DECORD_HYBRID_FORCE_SIDE=gpu`），repeat 3 / rounds 2 / cooldown 3：

| 窗口 | 档 | auto | side:gpu | Δ% | 符号 |
|---|---|---|---|---|---|
| 1000 | 冷启动 | 1.4722 | **1.0887** | −26.05% | --- |
| 1000 | **热池** | 0.9992 | **0.5947** | **−40.48%** | --- |
| 6000 | 冷启动 | 3.9018 | **3.5933** | −7.91% | --- |
| 6000 | **热池** | **2.8072** | 3.1312 | **+11.54%** | +++ |

### 读法与一处方法论说明
- **决策口径取热池**（引擎复用），与既有各轮一致。w=1000 热池：纯 GPU 臂快 **40.5%**；
  w=6000 热池：hybrid 快 **11.5%**（符号 +++ 一致）→ **交叉点被交错 A/B 确认在
  1000~6000 帧之间的 hevc 上**，与窗口曲线的方向一致。
- **冷启动行系统性偏向 B**：w=1000 冷 差 0.383s、w=6000 冷 差 0.309s，两者量级都≈
  进程级冷启动（TRT 反序列化 0.39~0.44s，C-36）。即**每轮先跑的 A 臂独吞进程冷启动**，
  与「更正五」同一机制。故冷启动行的**方向不可用于判据**，只作参照。

### 换算出的启动差（这是"稳定性"缺口的定量形式）
- hevc 稳态混合速率 2257 fps → 1000 帧理想 0.443s；实测热池 0.9992s ⇒ **启动摊销 ≈0.56s**。
- 纯 GPU 臂稳态 1873 fps → 理想 0.534s；实测 0.5947s ⇒ **启动摊销 ≈0.06s**。
- 差额 **≈0.49s**，即 **hybrid 比纯 NVDEC 多付约 0.5s 启动**，在 ≤约 1000 帧的短片上
  直接决定胜负。这与此前线性拟合（auto a≈0.99s vs 纯 GPU a≈0.59s，差 0.40s）互相印证。
## 深挖（同日续五）：启动摊销的根因已定位到 fork 的盲阶段路由（源码级证据）

用 `DECORD_HYBRID_DEBUG=1` + `DECORD_LIBRARY_PATH=<fork build>` 跑 hevc 600 帧，
抓到的前几条调度行：

```
[hybrid-sched] key=240240 rc-unknown -> CPU-sample      <- chunk 0 -> CPU
[hybrid-sched] key=480480 rc-unknown -> CPU-sample      <- chunk 1 -> CPU
[hybrid-plan] k0=3 chunks=97 frames cpu=30 gpu=67 (share=0.32)
```

### 根因
`ChooseSide()` 的盲阶段首判是：

```cpp
if (rate[SIDE_CPU] <= 0) { ... return SIDE_CPU; }   // 采样 CPU 产率
```

而 `rate[SIDE_CPU]`（`cpu_.ProductionRate()`，16 帧段速率首折才就位）在开头**连续多个
keyframe 都还是 0** -> **chunk 0 与 chunk 1 都被判给 CPU**；water-filling 计划直到
**k0=3** 才建立（此后才是 share=0.32 的混跑）。由于交付严格保序，**队头被慢腿
（hevc CPU 臂 816 fps）串行占住**，GPU 臂在启动期几乎无活可干（日志里大量
`[hybrid-u] cpu-empty` 即 CPU 队列空/被窄窗限制）。

### 与代码自述的意图不一致（这可判为缺陷）
同一函数上方注释写的是：

> rc 未学得：给一个 CPU chunk 采样……**该 chunk 与 chunk0 的 GPU 发射重叠，
> 且 GPU-first 下盲决策全落 GPU**，采样安全

即**作者假定首块走 GPU**；但实现里首判返回 CPU，chunk0 与 chunk1 都落 CPU
——**实现与其注释描述的形态相反**，这正是那约 0.5s 启动摊销的来源。

### 拟用修复（源码已试改，但**未重新构建 DLL**）
在 `ChooseSide()` 开头插入首块路由（10 行）：

```cpp
if (chunks_assigned_[SIDE_CPU] + chunks_assigned_[SIDE_GPU] == 0) {
    return SIDE_GPU;            // 首块恒走 GPU；CPU 采样仍由下一块完成
}
```

CPU 采样保留（下一块），与 chunk0 的 GPU 发射重叠 —— 正是注释假定的形态。

**未能落地的原因（工具环境限制，非技术风险）**：重编 `decord.dll` 需经
`vcvars64.bat`（.bat）建立 MSVC 环境，而本会话的指令策略**禁止从 Bash 工具
或 PS 工具调用 `cmd.exe`**（直接调 .bat 只开出交互 shell、不执行）；
且 Windows 会锁住已加载的 DLL，构建必须在无进程持有它时进行。
故本轮**已把 fork 源码回退到 HEAD**（`build-081fix/decord.dll` md5
`2f4c9a1a...` 经校验未变），保持源码与二进制一致，不留"两套真相"。

### 重建后的验证步骤（供下次执行）
1. `_build_busy.bat`（或 `cmake --build build-081fix`）增量构建 -> 记新 DLL md5。
2. `DECORD_HYBRID_DEBUG=1` 跑 600 帧：应出现 `first-chunk -> GPU` 且**首条
   `rc-unknown -> CPU-sample` 落到 key=480480**（即 chunk 1）。
3. 金标 `tests/golden/record.py --verify` 28/28（路由改动不影响比特）。
4. `bench ab --config hevc-hybrid --a auto --b gpu --window 1000`
   热池：预期 auto 由 0.9992 降到约 0.6 以下 -> **短片也反超纯 GPU 臂**，
   即目标（任何编码上都稳定快于单臂）达成。
### 附：一次未完成的旋钮扫描（环境/工具问题，非结论）

对 hevc 短片（w=1000）扫 `KICK_BURST` / `PREFETCH_DEPTH_HYBRID` /
`FFMPEG_THREAD_COUNT` / `UPLOAD_BATCH` / `NO_EXPECTED` 六个 env 臂时，**扫描在
13 分 24 秒处仍未结束**（正常应约 2 分钟：6 臂 x 3 reps x 1000 帧），最终被中止。
**无法判定是哪个臂挂住**——命令把 stdout 经 `tail -12` 收口，管道结束前无任何
输出，这正是"看不到中间态"的原因。故本条**只是观测，不是结论**：
既不能断言某个旋钮会挂，也不能断言与 fork 无关。

两项待办（下次）：
1. 扫描脚本**不要用 `tail` 收口**（改为逐臂即时 flush，或落盘 JSONL），
   否则单臂挂死会吞掉全部已完成臂的数据；
2. 若复现，按"逐臂单独跑 + 硬超时"定位（本仓 rules 已有"子进程须带硬超时"
   同源条款，此处不例外）。
## 深挖（同日续六）：fork 补丁**已构建并实测**——只拿到约 2%，启动假设需更正

### 补丁已落地（推翻"续五"末尾的"未能构建"记录）
利用 VS 自带的 MSVC 工具链（在 PS 工具里直接配 `PATH`/`INCLUDE`/`LIB`，
不经过 `cmd.exe`）完成**增量构建**：`1 编译 + 1 链接`，约 1 分钟。
fork 侧提交 `f946d72`（净 +5/−1 行），DLL 由 md5 `2f4c9a11…` 变为 `6367c5fa…`。

路由行为随补丁改变：`hybrid-plan` 的建立点由 `k0=3` 推迟到 **`k0=4`**
（盲阶段判定次数变化，符合预期）。

### 实测效果：只有约 2%，远小于预期的 0.5s
hevc 短片 1000 帧，热池交错 A/B（repeat3，符号全 `---` 一致）：

| 时序 | auto（补丁前） | auto（补丁后） | 纯 GPU 臂 |
|---|---|---|---|
| 冷启动 | 1.4722 | **1.3716（−6.8%）** | 1.0468 |
| 热池 | 0.9992 | **0.9799（−1.9%）** | 0.5903 |

即：**补丁方向正确但量级很小**（热池 −1.9%、冷启动 −6.8%），
与纯 GPU 臂的差距基本未变（−40.5% → −39.8%）。**"chunk1/chunk2 连续压慢腿"
不是那 0.5s 的主因**——据此更正「续五」的结论：那条只解释了其中约 2%。

### 更正机制：那不是"固定启动"，而是"速率爬升"
w=200（解码量可忽略，独立子进程，min-of-3）：

| 臂 | wall | decode | busy_c% |
|---|---|---|---|
| auto（hybrid） | 0.945 | 0.064 | 39.3 |
| side:gpu | 0.842 | 0.066 | 0 |
| side:cpu | 0.940 | 0.161 | 44.9 |

- 三条 wall 都在 0.84~0.95s，而解码只占 0.06s ⇒ 这部分是**进程/引擎级固定开销**
  （TRT 反序列化、CUDA 上下文、会话装配），**与解码路线无关**。
- auto 比 side:gpu 只贵 **0.10s**（不是 0.5s）⇒ **hybrid 的固定启动溢价很小**。
- 但 w=1000 热池里 auto 比纯 GPU 贵 **0.39s**（0.9799 vs 0.5903）。

⇒ 结论：那 0.4~0.5s 的缺口主要出现在 **解码量增大之后**，即 hybrid 的
**混跑速率需要若干千帧才爬升到稳态**（计划在 k0=3~4 建立，但两侧库存、
GPU 落地速率 EWMA、CPU 生产速率读数都要时间收敛）。
**它不是"固定启动摊销"，用固定 intercept 拟合会误读。**

### 正确性门禁（新 DLL）
`tests/golden/record.py --verify`：**28/28 一致**（含 C-hevc-hybrid）——
路由改动不影响比特，引擎侧 gpu/cpu 等价性与金标均保持。

### 下一步（按新机制）
靶子从"首块路由"改为**爬升速度**：
1. 量出爬升曲线（每 500 帧一档的瞬时速率，hevc w∈{500,1000,2000,4000,8000}），
   定位是"计划生效慢"还是"两侧库存填充慢"；
2. 若属前者：盲阶段按初始速率给一个**非对称起始分配**（而非等份额起步）；
3. 若属后者：查 `ready_`/`queue_` 起始深度与 GPU 落地 EWMA 的收敛速度
   （`gpu_rate_landed_` 每 16 帧一折、快升慢降）。
## 深挖（同日续七）：三编码窗口扫描——"稳定"边界定稿，缺口量级更正为约 0.17s

口径：`_probe_hybrid_reeval.py --window W --reps 2`（**每臂独立子进程、min-of-2**），
A=`auto`（hybrid 混跑）B=`side:gpu`（纯 NVDEC 臂）。同机同刻比较两臂，故共享的
进程/引擎固定开销（约 0.8s，见续六）在**差值**里被消掉。

### hevc（test6_hevc.mp4，23970 帧）
| W | auto | side:gpu | 谁赢 | auto 累计速率 | gpu 累计速率 |
|---|---|---|---|---|---|
| 500 | 1.099 | **0.930** | gpu +18% | 455 | 538 |
| 1500 | 1.431 | **1.276** | gpu +12% | 1048 | 1176 |
| 3000 | **1.948** | 2.066 | auto +6% | 1540 | 1452 |
| 6000 | **3.198** | 3.568 | auto +12% | 1876 | 1682 |
| 12000 | **5.824** | 6.612 | auto +14% | 2060 | 1815 |

分段速率（由相邻档差分，fps）：auto **1000 → 2901 → 2400 → 2285**；
gpu **1647 → 1899 → 1997 → 1971**。即**两臂都要爬升**，auto 的爬升幅度更大
（起点更低、稳态更高），在 1500~3000 帧区间反超。

### av1（test6.mp4）
| W | auto | side:gpu | 谁赢 |
|---|---|---|---|
| 500 | 1.152 | **0.959** | gpu +20% |
| 1500 | 1.454 | 1.434 | 持平 |
| 3000 | **2.001** | 2.320 | auto +16% |
| 6000 | **2.981** | 4.173 | auto +40% |

### h264（test5.mp4，7761 帧）
| W | auto | side:gpu | 谁赢 |
|---|---|---|---|
| 500 | **1.029** | 1.101 | auto +7% |
| 1500 | **1.776** | 2.159 | auto +22% |
| 3000 | **2.412** | 3.759 | auto +56% |
| 6000 | **3.268** | 6.893 | auto +111% |

### 结论（三编码统一）
1. **h264：无交叉点** —— 从 500 帧起 hybrid 就赢，且优势随长度放大（6000 帧 +111%）。
2. **hevc / av1：交叉点在 1500~3000 帧之间**（≈13~25 秒的 120fps 视频）；
   此后 hybrid 稳定胜出（6000 帧时 +12% / +40%）。
3. **短窗缺口量级 = 约 0.17~0.19s**（hevc W=500 差 0.169s、av1 差 0.193s），
   而 h264 在 500 帧就已领先 0.072s。

### 更正（本节推翻了"续四/续六"的量级判断）
- 先前用**固定 intercept 拟合**得出 hybrid"多付约 0.49s 启动"；本轮的窗口扫描显示
  真实短窗缺口只有 **约 0.17s**。固定截距拟合把"速率爬升"折成了截距，**高估了约 3 倍**。
- 先前 w=1000 的 bench-ab 热池读数（0.9992 vs 0.5947，差 0.39s）也偏大：那套口径
  在同一进程内跑两臂，残余的顺序/冷启动效应会记到先跑的臂上（同"更正五"）。
  **判定口径应以本节的「独立子进程 × 同刻两臂 × 差值」为准。**
- 因此：fork 侧那条首块路由补丁（`f946d72`，实测热池 −1.9%/冷启动 −6.8%）
  **是本轮唯一确定的正向改动**，但它不是缺口主因；主因是**前约 1500 帧的速率爬升**。

### 剩余可攻的靶子（按量级）
1. **CPU 腿自启动开销 ≈0.10s**（续六 w=200 实测：auto 0.945 vs side:gpu 0.842，
   而 side:cpu 0.940 ⇒ CPU 腿自身就带约 0.10s 的装配/首帧成本）。若能**在首个
   CPU chunk 之前启动 CPU 腿**（预热线程池/解码器而不占用交付顺序），这 0.1s 可省。
2. **前 1500 帧的混跑份额未达稳态**：auto 在 500~1500 帧的分段速率只有 1000 fps
   （稳态 2285），而 gpu 臂同期 1647 fps。可查 `rate[]` 的收敛速度与
   `plan_ready_` 之前的分配（当前计划在 k0=4 才建立）。
## 深挖（同日续八）：剩余 0.17s 的构成已定位 = CPU 首块暴露（t_cpu(chunk1) − t_gpu(chunk0)）

### 极小窗口隔离（hevc，`--reps 3`，独立子进程）
| W | auto | side:gpu | side:cpu | auto−gpu | cpu−gpu |
|---|---|---|---|---|---|
| 1 | 0.852 | 0.869 | 0.897 | **−0.017** | +0.028 |
| 50 | 0.866 | 0.770 | 0.873 | **+0.096** | +0.103 |
| 200 | 0.920 | 0.832 | 0.937 | +0.088 | +0.105 |

- **W=1（纯装配）：auto 与 side:gpu 无差（−0.017s，噪声内）** ⇒ hybrid 的双上下文/
  计划/线程装配**不额外花钱**；三臂共同的约 0.85s 是进程+引擎级固定开销。
- **W=50：decode 只占 0.001s，但 auto 已比 side:gpu 贵 0.096s** ⇒ 这笔钱不在解码量，
  而在**盲阶段那个 CPU 采样块的等待**（CPU 腿初始化 + 该块落地）。

### 与 500~1500 帧区间对齐，构成自洽
- W=500 差 0.169s、W=1500 差 0.155s（≈常数）⇒ 差额是**一次性**的，与前 1500 帧
  的累计解码量无关。
- 数量级核对：hevc 块 ≈247 帧；`t_cpu(块)` ≈ 247/816 ≈ **0.30s**，
  `t_gpu(块)` ≈ 247/1873 ≈ **0.13s**，暴露 ≈ **0.17s** —— 与实测 0.169s **吻合**。
- 即：**剩余缺口的全部 ≈ = 慢腿首个采样块在保序交付上暴露的时间**。

### 下一步（三条，按可行性）
1. **带外采样 CPU 产率**（最彻底）：解码器初始化时，用一次性 16 帧丢弃式软解
   探针给 `rc` 播种 ⇒ 计划立即成形、盲阶段无需占用交付序上的 CPU 块 ⇒ 该
   0.17s 可整体消除。代价：新增代码与线程（须不与 demux 竞争）。
2. **非对称预热**（简单但需速率感知）：让 GPU 先吃 2 块再做 CPU 采样，暴露降为
   ≈0.04s；但对 **h264**（CPU 腿 2047 fps 远快于 GPU 885 fps）会**反向伤害**
   —— 必须先能判"哪条腿快"。盲阶段无速率，故需先解决第 1 条的播种问题。
3. **不加改动**：接受 0.17s；本仓实际素材（7761~23970 帧）均已远超交叉点。

### 结论（对目标的现状判定）
用户目标"hybrid 在任何编码上都**稳定**快于单臂"：
- **对纯 NVDEC**：h264 从 500 帧起即胜（无交叉点）；**hevc/av1 自 1500~3000 帧
  起稳定胜出**；此前缺口 **0.17s**（一次性，已定位到具体成分）。
- **对纯 CPU 臂**：三编码全片口径均已胜（1.10× / 2.76× / 1.81×）。
- 故"稳定"的唯一残留条件 = 视频长度 ≥ 约 2000 帧（≈17 秒 @120fps）；
  更短的内容需要靠上面第 1 条（带外播种）才能消除。
## 深挖（同日续九）：一个必须先说清的前提——默认加载的不是本地 dev dll

```
安装包内 decord.dll            md5 4116fb4a…   478208 B   ← 默认加载（pip wheel v0.8.3）
D:\Repo\decord\build-081fix\decord.dll  md5 6367c5fa…  478016+?  ← 仅当设 DECORD_LIBRARY_PATH 时加载
```

- 本仓工具链（`tools/bench.py` / `tools/_probe_hybrid_reeval.py`）会显式设
  `DECORD_LIBRARY_PATH=<build 树>`，故**本轮所有 hybrid 数字都跑在本地 dev dll 上**
  （该设定确实生效：`[hs]` 诊断行只在装入本次构建的 dll 时出现）。
- **产品默认路径加载的是 pip wheel 里的 dll（`4116fb4a…`）**，也就是 v0.8.3
  已发布的那份——**不含** `f946d72` 这条盲阶段修正。
- 故本次修正若要进入产品，须**重建并安装 wheel**（fork 已有 wheel 流水线；
  本文件"已知问题"节已记：本地 wheel 与 CI 产物 sha16 不同，属既有现象）。

### 对结论的影响（须一并读）
- 「窗口扫描（缺口 0.17s、交叉点 1500~3000 帧）」是在**已打补丁**的 dev dll 上测的；
  未打补丁时 W=1000 热池略差（0.9992 vs 补丁后 0.9799，约 +0.02s），
  即**产品现状的短窗缺口约 0.19s，交叉点位置不变**（该量级差异不足以移动交叉点）。
- 「vs 纯 CPU 臂」（1.10× / 2.76× / 1.81× 全片）与「vs 纯 NVDEC 的稳态优势」
  都在全片口径，**不受此 0.02s 影响**。
## 深挖（同日续十）：两套 harness 的缺口为何不同——"冷 0.17s / 热 0.37s"（状态相关）

现象：同一对臂（hevc、W=1000、ocr=trt）在**两套 harness** 下给出不同缺口：

| harness | auto | side:gpu | 缺口 |
|---|---|---|---|
| `bench ab`（同进程、repeat 3、热池=引擎复用） | 0.9598~0.9799 | 0.5903~0.5944 | **0.37~0.39s** |
| `_probe_hybrid_reeval.py --reps 2`（每臂独立进程、min-of-2） | — | — | **≈0.17s**（W=500 0.169 / W=1500 0.155） |

### 先排除"顺序伪影"
把 `ab` 的两臂**反序**再跑一次（A=gpu、B=auto，`--env-a FORCE_SIDE=gpu`）：
热池 gpu 0.5944 / auto **0.9598**（+61.5%，符号 `+++`）——缺口与正序（0.39s）**同量级**。
⇒ **不是顺序伪影**，`ab` 的读数在两序下自洽。

### 归因（已排除几何差异）
两套用的**同视频同 ROI**（`test6_hevc`，841,994,949,1026），故不是几何问题。
差异在**引擎是否预热**：

- `bench ab` 的热池档：**引擎池已热** ⇒ OCR 消费快 ⇒ 管线**由解码侧binding**
  ⇒ 慢腿采样块在保序交付上的暴露（约 0.3s 量级）**完整显形**。
- 独立进程档：引擎**冷启** ⇒ 消费侧（TRT 反序列化 + 会话装配，约 0.8s，见续六）
  远慢于解码 ⇒ 解码侧的停顿被**吸收/掩盖**，只露出约 0.17s。

即两个数都真，只是**测量状态不同**。

### 结论（修正"续七"的表述）
- **短窗缺口 = 冷态 ≈0.17s，热态 ≈0.37s**；**生产口径（引擎池预热、反复抽取）
  应取热态 0.37s**——这也解释了为什么我先前用固定截距拟合得到 ≈0.49s：
  那个拟合混入了同一进程的顺序/状态效应，**方向对但偏大**。
- 交叉点：热态下用"稳态优势 0.16s/千帧"对"0.37s 固定缺口"求交点 ⇒ **≈2300 帧**
  （冷态扫描给出 1500~3000 帧）——两者一致于 **约 2000~3000 帧**。
- 故"稳定性"缺口的**量级**应记为 **0.17~0.39s**（依引擎预热状态），
  而**交叉点位置**在两种口径下都落在约 2000~3000 帧。
## 深挖（同日续十一）：补丁对 h264 的影响方向不一致（须记为"待更多重复定性"）

方法：把 dev dll 在**补丁前**（md5 `2f4c9a11…`）与**补丁后**（`6367c5fa…`）之间
来回替换（每次换完记 md5 核对），跑同一对臂 `auto` vs `side:gpu`
（h264，`--reps 3`，独立子进程）。`side:gpu` 是同批的**对照臂**（它不受补丁影响）。

| W | 补丁前 auto | 补丁后 auto | 对照臂 side:gpu（前→后） | 读法 |
|---|---|---|---|---|
| 500 | 1.172 | **0.995（−15%）** | 1.056 → 1.093（+3.5%） | 补丁后 auto **由劣转优** |
| 1000 | **1.281** | 1.436（+12%） | 1.613 → 1.601（−0.7%） | 补丁后 auto **或退化** |

- 对照臂两次漂移仅 +3.5% / −0.7% ⇒ 该 harness 的噪声约 3%，两处变化都在噪声之上。
- 但**方向相反**：W=500 明显改善（且把"gpu 更快"翻成"auto 更快"），
  W=1000 反向退化 12%。**不能据此宣称补丁对 h264 是净正**。

### 处置
- 补丁**保留**（hevc 侧两次独立测量均为正：热池 −1.9%、冷启动 −6.8%；金标 28/28；
  h264 的方向未定且量级小），但**在装 wheel 进产品前**必须先做一次
  `bench ab` 交错 + 更多重复（≥5 轮）把 h264 的符号定下来。
- 这也说明：盲阶段路由对"CPU 腿更快"的编码（h264）本就处在**与该策略相反的 regime**，
  现策略（只采样一块后回 GPU）对 h264 未必是优解 —— 见"下一步"第 1 条（带外播种）
  能同时消除两边的取舍。
