# 2026-09-17 性能监测系统重设计：降噪（协议层）+ 提分辨率（仪器层）+ trace 实验

> 问题（用户）：重新设计该项目的性能监测系统，减少噪声，提高分辨率。
> 用户裁决：范围=全面改造（bench 协议 + 引擎遥测）；trace/timeline 值得实验，
> 成本守卫可放宽但**生产代码必须能无成本关闭**；验收实验 = §7 遗留的
> DECODE_THREADS=10 vs 32 档位判定。
> 设计依据：同日上午《流水线分级与瓶颈定位》§8 的测量学调查（方向已封板、
> 未落地）+ 三路代码勘察。**统计框架（交错+配对差分+--aa 标定+符号多数）
> 判定"本身正确"——本轮不推翻它，改的是喂给它的单次读数。**

## 0. 改动总览

| 层 | 改动 | 降噪/提分辨率 | 证据 |
|---|---|---|---|
| P0 探针 | `_probe_cycle_quant` / `_probe_clock_gate`（§8 固化，铁律 7） | 依据生产 | bench/cycle_quant.json、clock_gate.json |
| P1 bench | 每轮 GPU 时钟入账 + 时钟门禁 + ab 臂序轮转/自动判定/--aa | 降噪（判据层+协议层） | registry gpu 字段、ab 输出 |
| P2 引擎 | cycle 计数 / TOTALS n+max / span 卫生 / 覆盖缺口 / report v4 | 提分辨率（仪器层） | 下文 §2/§3 |
| P4 实验 | `VOE_TRACE_FILE` 全事件时间线 + 视图探针 | 提分辨率（时间维） | §4 |
| P3 标定 | telemetry-check --aa / ab --aa 重标 + 档位验收实验 | 分辨率落地 | §5 |

## 1. P0：§8 证据固化（先量后做）

`_probe_cycle_quant.py`（bench/cycle_quant.json）：
- `process_time()` 20 万次重采**只有一种非零增量 0.015625s**（1/64s）——
  §8.4 的量化断言从"278 万次采样"复检为确定性事实。
- `QueryProcessCycleTime` 语义：sleep 0.5s 增量仅 0.1ms 等价（=CPU 时间）；
  线程隔离实验（主线程 sleep、worker 忙）证明进程计数=各线程之和。
- cycles/wall CV **0.014%**（corr 0.9988，比 §8.1 的 0.701% 更静的窗口）；
  **自校准频率换算**（全程 Δcycles/Δprocess_time）双窗口一致到 0.017%
  （2.493 vs 2.493 GHz）——resources.py 换算法的合法性证明。
- 附注：four_threads 实验撞 GIL（纯 Python 线程串行），恰与 §6 的
  gil_check 结论同向；"求和语义"以 thread_isolation 项为准。

`_probe_clock_gate.py`（bench/clock_gate.json）：
- 冷轮（round 0）sm_min 450MHz、wall 3.74s；热轮（1..7）sm_min 恒 2700、
  wall 3.0955~3.1060s。CV 含全部 6.647% → 弃前 2 轮 0.120%（**×55.6**，
  复测 §8.2 的 54×）。
- **门禁比率校准（关键发现）**：`nvmlDeviceGetMaxClockInfo` 给的 3105MHz
  是 boost 上限，负载热平台只有 2700（=0.870×max）。k=0.85 仅剩 2.3%
  余量、k=0.9 会把热轮全拒；冷轮 450/3105=0.145。分离带 [0.145, 0.870]
  宽阔，**取 k=0.75**（两侧 >10pp 余量）落进 bench.py。
- throttle 位全程为 0——主动压频位在本负载从未触发，门禁的主动信号是
  sm_min；throttle 位作为次级判据保留。

## 2. P1：bench 协议与判据降噪（tools/bench.py）

1. **每轮 GPU 时钟入账**：`cmd_run` 每轮包 `NvmlSampler`，registry 记录
   新增 `gpu: {sm_min, sm_p50, sm_max, throttle_bad, valid, gate}`。
   `_gpu_state()` 从 nvidia-smi 子进程（43ms/次）改 NVML 直读。
   纯 CPU 配置（decode∈{cpu}∧ocr=cpu）不判 GPU 时钟（空闲时钟恒低，
   判了只会全 invalid 再回退），gate 记 `off:纯CPU配置不判GPU时钟`。
   历史记录无 gpu 字段一律视为 valid（**不追溯改判**）。
2. **时钟门禁**：valid = 无主动压频 tick ∧ sm_min ≥ 0.75×最大SM 时钟；
   采样点不足判 valid（fail-open：门禁不能无证据剔除）；热轮聚合只收
   valid 轮，全剔则回退全收并警告（门禁不能替无数据）。
   `--no-clock-gate` 逃生门。记账修正：引擎冷启动的第 1 轮是既有排除
   口径，不计入"门禁剔除"数。
3. **ab 加固**：臂序逐对轮转（AB/BA 交替，对消轮内位置效应——先跑臂
   系统性慢 ~1.3% 的结论从 telemetry-check 推广到 ab）；自动判定 =
   逐对热轮中位配对差分 → 均值+SE+符号多数（≥0.70）vs `--hard` 阈值，
   B 显著慢 → 退出码 1；`--no-verdict` 逃生门；`ab --aa` 两臂同配置 →
   子进程协议噪声带标定。逐 span 归因表只展示不判（C-42：占比差分
   ≠可回收量）。
4. `--priority above_normal`（opt-in 默认关）：子进程自提优先级
   （`SetPriorityClass`，伪句柄 -1 直传绕开 restype 截断陷阱）。
5. `PI15_FLOOR_PCT` 抽成常量（原 0.30 硬编码在 `_limit_from_band`）。

冒烟验证：冷轮被标记剔除（sm_min 690 < 2329 = 0.75×3105）、热轮保留；
ab --aa 轮转/聚合/标定链路全通。

## 3. P2：引擎遥测提分辨率（只加键，report v3→v4）

1. **cycle 计数**（resources.py）：`_HostCounters` 加
   `QueryProcessCycleTime`；`per_phase()` 新增 `cores_avg_cycles =
   (Δcycles/Δwall) ÷ f_run`，`f_run = 全程 Δcycles/Δprocess_time`
   自校准（P0 证明双窗口一致 0.017%）。旧 `cores_avg` 保留（长相位仍准）。
   **实测对照**（p2-verify 轮）：GPU calibrate 相位旧口径 0.0（±100%
   量化伪影）→ 新口径 0.31；host decode 相位 16.42 vs 16.38（长相位
   两口径一致，互为校验）。
2. **TOTALS 全键 n/max**（metrics.py）：`PROFILE_TOTALS_N/_MAX` 从 2 个
   队列键扩到全部 7 键（consume_feed/emit.*/merge_pair +10 注册名），
   "偶发长停顿 vs 持续细碎"分诊口径推广。注册表 48→50 项（≤64）。
3. **span 卫生**：`gpu_calib_total` 的 t0 从 `_t_open` 改到 open 收口处
   （消除 pipeline.calibrate 与 pipeline.setup 的双计）；死指标出清 8 个
   （pipeline.producer、emit.h2d_bytes、emit.launches、pools.high_water、
   frame_batch.inflight/leaks、segment.emit/emit_batch——grep 全仓零产出，
   删除后报告外观不变）；审查后**不改**：注入引擎路径的 PI-10 短路
   （reuse=0 → 不判）是正确语义，伪造 engine_init=0 反而失真。
4. **覆盖缺口**：`device.py` CPU 解码分支补 decode_batch/gray_batch/
   stream_analyze（键语义与宿主对齐）；宿主补 consume_feed（每帧）/
   merge_pair / 'open' L1 边界；GPU `_put_q` 补 q_put_block（生产者背压
   此前无对应物）。实测新增覆盖立即兑现：GPU q_put_block 3001 次
   共 0.004s（生产者从不阻塞的直接证据）。
5. **report v4**：`span_relations`（静态父子表+路径标记）+ 派生 span
   `<parent>_other`（= parent.sum − Σ在场子项 sum，子项互不重叠硬约束：
   宿主 consumer 只减 decode.* 与 consume_feed，merge_pair/q_put 嵌套
   在 consume_feed 内不单列）。**产品化上午 §6 的手工算术**：上午
   "consumer 缺口 0.70s 只解释 3%"，本轮 consume_feed 入账后
   `pipeline.consumer_other` 仅剩 0.088s——缺口归因闭合。
6. **测试**：test_report 快照 v4 + 关系派生用例；test_metrics 死键清单
   同步 + N/MAX 成对断言；test_telemetry_cost 重放口径更新
   （+2200 TOTALS 逐事件计数，键 10→14/8→10），**≤2ms/run 上限不变**。

正确性门禁：两路径段数 1090/1090（与上午基线一致），成本守卫 36/36 过。

## 4. P4：trace/timeline 实验（用户裁决；宽成本、默认关、关=零成本）

- 旋钮 `diag.trace_file`（env `VOE_TRACE_FILE`，默认空=关，铁律 10）；
  `video_ocr_engine/domain/trace.py`：线程局部桶 `(name, t0, t1, tid)`，事件源 = `_prof_end`
  脊柱（**不改签名**，t1 复用已算好的 t0+elapsed）+ 编排三段近似锚点 +
  顶层 pipeline.run 真实锚点；开启时自带 NVML 0.2s 点列（同 perf_counter
  时间轴）。嵌套父子离线按同线程区间包含重建。
- **关闭零成本（结构性）**：`_prof_end` 尾部一次 `is not None` 判断；
  std 档 ≤2ms/run 守卫在 trace 代码在场但关闭时原样通过 = 守护。
  开启成本单测宽上限 50ms/2300 事件（实测 ~1ms 级；用户裁决放宽）。
- 视图探针 `_probe_trace_view.py`：文本甘特（60 桶密度）+ 线程重叠矩阵 +
  空洞清单 + NVML 时钟排 + on/off 成本对照。
- **验收（两路径，段数 1090/1090 一致）**：
  - host（cpu+openvino）：7363 事件/382KB；**会话线程忙 94.2% 其中
    q_get_wait 1.510s**、双推理线程各 ~65% 忙且互相重叠 45.4%（OpenVINO
    双实例并行结构直接可视）——聚合报告给不出的并发图。
  - GPU（nvdec+trt）：9494 事件/481KB；**decode.batch 2.938s 占满生产者
    线程（95.5%）**、会话线程 q_get_wait 3.019s（97.7%）、推理线程仅
    14.1% 忙、sm_clock 全程 2700 无压频——上午 §4 的全部结论在时间线上
    直接复现。
  - 成本：host on/off +1.14%、GPU +0.39%（**未交错**的同序两轮，落在
    噪声带内，不能当结论；µs 级单测 ~1ms/2300 事件）。NVML 采样线程
    +每事件一次 append 的墙钟影响在分辨力之下。
- **裁决：保留为 opt-in 实验工具（默认关）**。理由：诊断价值已被两路径
  验收证明，但常开成本含 NVML 线程与落盘（~0.5MB/run），且日常 A/B 的
  判据用不到时间线；按需开启（`VOE_TRACE_FILE=...`）零仪式感。
  转正（默认开）需要一次交错 A/B 级的成本证据，留复评。

## 5. P3：重标定与验收实验

### 5.1 PI-15 重标（新增遥测后的合规动作）

`telemetry-check --aa`（h264-gpu，n=25/inner=3，同进程交替）：均值 −0.038%
（旧 +0.072%）/ |Δ|p50 0.089% / |Δ|p95 0.734% / sd 0.286% / SE 0.057%。
|均值|+3×SE = 0.209% < 0.30% 地板 → **阈值维持 std +0.30% / full +1.20%**：
新增遥测面（TOTALS n/max、覆盖缺口、trace 关闭态判断）没有把噪声带推过地板。

### 5.2 ab 子进程协议 A/A（新协议：臂序轮转 + 时钟门禁 + 冷热分离）

- GPU（h264-gpu+trt，8 对）：均值 −0.241%、sd 0.254%、|Δ|p95 0.517% →
  建议 `--hard 0.55%`。对照旧口径：子进程 A/A |Δ|p95 2.33%（2026-09-11）、
  `_probe_dll_ab` 冷启 sd 4.6%——热轮口径下带收窄约一个量级。
- CPU（h264-cpu+openvino，8 对）：均值 −0.131%、sd 1.220%、p95 1.291% →
  建议 `--hard 1.45%`（纯 CPU 路径宿主调度敏感，共享桌面噪声面更大）。

### 5.3 验收实验：DECODE_THREADS=10 vs 32（上午 §7 遗留问题的裁决）

协议：`bench ab --config h264-cpu --ocr-backend cpu --repeat 8
--env-a DECODE_THREADS=10 --env-b DECODE_THREADS=32 --hard 1.45`
（臂有效性证据 = 两臂 decode.batch 结构差 5.6×，非静默回落）。

- **热池判定：不可判定**（均值 +0.97%、SE 2.23%、阈值 1.45%、符号 4/4
  无多数）→ 档位 10→32 无墙钟收益。
- **冷启动：t32 一致慢 +4.03%（8/8 符号）**——32 解码线程在引擎构建期
  有一致性损害。
- **机制**（span 归因；C-42：归因≠可回收）：t32 的 `decode.batch` 阻塞
  −82%（1.380→0.245s）、`q_get_wait` −96%（1.292→0.046s），但
  `consume_feed` +120%、`preprocess` +79%、`infer` +35%、`merge_pair`
  +65%——**瓶颈从解码移到 GIL 争用**（与上午 gil_check 判定同向：
  numpy/OpenVINO 内核被同进程解码线程串行化），净收益被完全吃掉。
- 上午 §7 不自洽的解：引擎内 `decode.batch`（1.356s）低于 10T 独立扫描
  （1.539s）= decord 内部预取线程使 get_batch **阻塞时间** < 独立扫描
  **总时间**——两口径本就不可直接比。
- 结论进 C-50：**维持 10 档**；档位依据从"ONNX 时代实测"更新为本轮
  交错 A/B（方向未变：升档无益且冷启有害）。

## 6. 否决/缓议记录（备查）

- **换时钟/锁频**：§8.1（cycles/wall CV 0.014% 复测强化——噪声是调度
  不是降频）与 §8.5（`-lgc` 无权限）证据否决；CUDA event 仅 GPU 子相位。
- **diff 加 per-span 硬门禁**：C-42 误读风险；span 归因只展示。
- **trace 默认开**：见 §4 裁决，留复评。
- **全量重写 `_gpu_state` 之外的 registry schema**：registry 是 append-only
  历史，新增 gpu 字段按"历史无字段=valid"兼容读取，不版本化。

## 7. 续轮（同日第二段）：仪器闭合 + trace 转正裁决

> 问题（用户）："若要继续改进遥测，还有哪些可以做的？"→ 选做仪器闭合
> （W1 直方图 / W2 子相位 / W3 相位时钟）+ trace 转正评估（W4）。

### 7.1 W1：TOTALS 分布形状（report v5）

- **分箱设计**：按 2 的指数分箱（桶号 = `math.frexp(x)[1] + 20`，32 桶
  覆盖 ~0.95µs~2048s）。**为什么不用 16 个边界做循环扫描**：原型实测
  逐边界扫描 0.5µs/事件、乘数法 0.17µs/事件、frexp 0.165µs/事件——
  无界键的事件量是 6270/run，边界扫描会多吃 ~2ms。
- **档位约束（关键决策）**：直方图**只在 full 档插桩**。6270 事件 ×
  0.165µs ≈ 1.0ms，而 std 档的整条遥测路径预算是 2ms/run（当前实测
  0.691ms，余量 65%）——把直方图放进 std 会吃掉一半余量，为"形状信息"
  付这个价不值（std 档已有 n/max 两端）。full 档墙钟预算本来就是 ±1.2%
  量级，付得起。守卫 `test_full_tier_histogram_cost_is_bounded`（宽松
  上限 8ms = 实测 ~8 倍，只抓病理回退）+ 桶和 = 事件数（不丢事件）。
- report v5：`histograms`（仅 full 档出现，缺席≠空值）+ `histograms_meta`
  （分箱自述：n_buckets / exp_bias / 各桶下界秒值），读者可自证算术。
- **首次读数即产出新证据**（host full 档）：
  - `pipeline.q_get_wait_hist` 双峰清晰：244µs 档 129 次（正常等待）
    vs 7.8ms 档 68 次 + 15.6ms 档 60 次——**后者是 `process_time` 的
    15.625ms tick 整数倍**，即"等待期间宿主被调度器换出"的量化指纹
    （§8.4 的 tick 现象在事件分布上直接可见，v4 的 n/max 只看得到
    `max 41.6ms` 这个孤值，看不出"60 次都是同一个 tick 值"）。
  - `segment.merge_pair_hist`：503 次在 122µs 档、105 次在 488µs、
    53 次在 977µs——相似判定**本身**是双峰（内容变/不变的两种路径），
    v4 的 `max 3.2ms` 完全看不到这个结构。
  - GPU `emit.put_hist`：793/1090 集中在 15µs 档、尾部 8 次 >977µs。

### 7.2 W2：子相位闭合（`_other` 从"缺口"变"残差"）

- `ocr.infer` 内部此前只有 `ctc_decode` 一个子键，host 路径
  `ocr.infer_other` 高达 **2.83s**（占该 span 的 92%）——"缺口"其实混合
  了 TRT 提交/规约/拼接三件事。新增三键（**full 档专属**，per-子批 ×
  数十批属热路径）：`ocr.trt_enq`（execute_async_v3 提交）/
  `ocr.trt_reduce`（argmax 规约 + D2H，含流同步=等 GPU）/`ocr.trt_concat`
  （多子批拼接，纯宿主）。
- **效果**（GPU full 档）：`ocr.infer_other` **0.456 → 0.073s**（归因
  84%），子相位自身给出结构：`trt_enq 0.2502s` / `trt_reduce 0.1236s` /
  `trt_concat 0.0000s`——**提交（宿主侧）是等待（设备侧）的 2 倍**，
  与 C-47"残差=GPU 链争用主导"方向一致但更细：宿主被调度饥饿的时间
  超过等 GPU 的时间。
- relations 表扩展：host `consume_feed ⊃ merge_pair + q_put_block`
  （此前这两键挂在 consumer 下但实际嵌套在 consume_feed 内，v4 未声明）。
  **宿主实测闭合**：`consume_feed 0.5075 − merge_pair 0.2647 −
  q_put_block 0.0232 = consume_feed_other 0.2197`（状态机裸成本）——
  与派生值逐位一致，关系表算术自证。
- 边界（写进代码注释）：`submit_argmax_deferred` 的深度 2 路径**未打桩**
  （默认关；开时 `infer_other` 会重新变大，读者知悉）。

### 7.3 W3：相位边界带时钟（`sm_mhz_at_start/end`）

`_HostCounters.sample()` 在 NVML 会话**已存在**时读一次
`nvmlDeviceGetClockInfo(SM)`——GPU 路径必然已初始化（环境指纹/采样器），
**宿主纯 CPU 路径零成本不触发初始化**（`_NVML["state"]` 缓存态判据，
与 `_vram_bytes` 同款纪律）。`per_phase` 输出相位首尾两个读数：
"这一相位跑在什么时钟上"随报告自证，不必再事后跑 NVML 探针。
GPU 验证轮读数：decode 相位 2700/2700（热平台，与 P0b 标定一致）。
sources 里自述来源与"不主动初始化"的边界。

### 7.4 附带修复：`bench run --telemetry` 从未生效

W1 排查"full 档为什么没有 histograms"时暴露：`_round(..., telemetry, ...)`
**收下参数却从未使用**——引擎档位实际由 `VOE_TELEMETRY` 环境变量决定，
所以历史 `bench run --telemetry full` 全部静默按 std 跑（此前无功能依赖
它，故未被发现；报告 `tier` 字段一直是诚实的，只是与命令行不符）。
修法：`_round` 内 `os.environ["VOE_TELEMETRY"] = telemetry` + try/finally
复原（与 telemetry-check 的同进程口径一致）。**影响面**：历史 registry
里标 `"telemetry": "full"` 的记录其 `report.tier` 是 std——按 tier 字段
读报告的历史分析不受影响（tier 一直如实记录），按命令行参数读的需知悉。

### 7.5 W4：trace 转正裁决（交错 A/B 口径）

协议升级：前一轮只有"未交错同序两轮"（+0.39%/+1.14%），证据等级不足。
本轮按 `bench ab` 交错口径（臂序轮转 + 时钟门禁 + 热池分离）：

| 路径 | 对 | 热池 Δ% | 逐对符号 | 判定 |
|---|---:|---:|---|---|
| host（cpu+ov） | 8 | +1.36% | `+--++-++` | 不可判定（SE 1.16%，限 1.45%） |
| host（重跑） | 8 | +2.22% | `-+++++++` | 不可判定（符号 5/3） |
| **GPU（nvdec+trt）** | 8 | **+0.31%** | `++++++++` | 不可判定（均值 0.453% < 限 0.55%） |

- GPU 那份是三份里最干净的：**符号 8/8 一致指向 trace 略慢**，但幅度
  低于本机 GPU 协议的可分辨下限（0.55%）——即"有方向、无显著性"。
- host 两份符号都乱（`+--++-++`/`-+++++++`），说明其 +1.4~2.2% 的差值
  被共享桌面的宿主调度漂移主导，不能归给 trace。
- 口径提示：本轮 host 协议 A/A（6 对）实测 sd 2.63%，明显宽于上一轮
  8 对的 1.22%——**共享桌面的噪声面本身在漂移**，跨轮引用阈值需谨慎
  （这正是 C-51 把"换窗口/换机器重标"写进复评触发的原因）。
- **裁决：维持 opt-in（不转正默认开）**。理由：GPU 路径的方向性证据
  （8/8 符号、+0.3% 量级）已足够说明"在有预算敏感的 A/B 里不该默认开"，
  但也不构成"必须禁用"——诊断场合按需开启即可。转正若要做，需要
  锁频环境或 n≥30 才能给出显著结论。

### 7.6 PI-15 重标（本轮新增遥测的合规动作）

`telemetry-check --aa`（n=25/inner=3，同进程，h264-gpu）：均值
**+0.022%**、|Δ|p50 0.146%、|Δ|p95 **0.654%**、sd 0.421%、SE 0.084%
→ |均值|+3×SE = **0.274%** < 0.30% 地板 → **阈值维持 std +0.30% /
full +1.20%**。三轮标定对照（同协议）：p95 0.484%（09-13 重标）→
0.734%（本轮第一段）→ 0.654%（本轮续段）——新增直方图/子相位/时钟
读数都**只在 full 档**，std 档遥测面未变，标定结果稳定，地板一直绑定。

## 6bis. 续轮结论（C-52）

新增 C-52（监测系统续轮基线）：直方图只进 full 档（预算决策）、子相位
闭合把 `_other` 变残差、相位时钟自证、`bench run --telemetry` 修复、
trace 维持 opt-in（GPU 8/8 符号 +0.31% 低于可分辨下限）。

## 8. 三段续（同日第三段）：归因显著性与冷启动判定（W5/W6）

> 承接：§7 之后复盘 W5 归因表时发现**配对塌缩 bug**——自查发现，非用户
> 反馈（此轮全程无用户指令介入）。

### 8.1 W5 的 bug 与修复（先承认错误）

首版 `_paired_metric_diffs` 用**裸 round 号**做配对键：`{r: rep for ...}`。
但每对 A/B 各有独立的 round 1/2，`--repeat 6` 的 12 份报告被塌缩成
{round1: 最后一份, round2: 最后一份} 两个键——表里所有键的 "n=2" 实为
"两个塌缩伪样本"，§7 时代价是同码都能"显著"出 −44%±52% 的伪强信号。
**修复**：配对键 = (对序号, 轮号)，对序号从 label 的 `#i@runid` 解析；
热轮口径与 wall 判定一致（round>1 ∧ 时钟门禁 valid）；符号多数的分母
改用该键的有效 n（此前错用请求对数）。教训入档：**给统计判据写测试时
必须包含"同码无信号"的 A/A 用例**——只测"有信号时能出表"测不出配对错。

### 8.2 修复后的真值对照（C-50 机制，6 对）

用已封板的 DECODE_THREADS=10 vs 32 做验证（这是现成的"wall 判不了但
机制已知"的黄金用例）：

| 层 | 读数 | 对照 C-50 |
|---|---|---|
| wall（热） | 不可判定：−1.32% ± 1.44%，符号 3/3 | ✓ 一致 |
| **冷启动判定（W6 新增）** | **B 显著慢 +5.78%（6/0 符号，限 4.0）** | C-50 冷启 +4.03%（8/8）同向 |
| 归因 `decode.batch` | **−80.52% ± 0.26%，0/6 `**`** | C-50：−82% ✓ |
| 归因 `q_get_wait` | −93.2% `**` | C-50：−96% ✓ |
| 归因 GIL 链 | consume_feed +156% / preprocess +76% / infer +43% / merge_pair +57%，全 `**` | C-50：+120/+79/+35/+65% ✓ |

三层口径首次在同一次 A/B 里互补工作：wall 判不了 → 冷判定抓冷启退化 →
归因给出带显著性的机制全貌。归因表样本量门（有效配对 < max(3,⌈pairs/3⌉)
不出表）与 "--repeat ≥6" 提示同时生效。

### 8.3 W6 标定与落地（benchmarks.yaml:ab_gate_calibration）

`ab --aa` 现在同表输出冷带（冷=进程首个 extract，含引擎构建+时钟爬坡）：

| 配置（6 对） | 热带 sd / 建议 --hard | 冷带 sd / 建议 --hard-cold |
|---|---|---|
| h264-gpu | 0.277% / **0.40%** | 2.231% / **3.40%** |
| h264-cpu | 1.399% / **1.75%** | 2.114% / **3.95%** |

冷带幅值是热带的 4~8 倍（引擎构建 0.4s 占冷轮 ~15%，其自身跨进程方差
大）——**冷判定必须用独立阈值，不能复用热带的**。默认 `--hard-cold=0`
（不判冷，先标定后门禁）；两配置建议值已入 `benchmarks.yaml` 与
`rules.yaml` 注释。

### 8.4 判据语义边界（防误读，写进代码 docstring）

- 归因显著性（`**`）= "这个指标的配对差分 CI 排零且方向稳定"的**统计
  陈述**，不是门禁（不产退出码），更不是可回收量（C-42）。
- 微小基数的百分比放大：`q_put_block_max` 基数 0.0002s，+21713% 这种
  数字真实但不可按幅值排序解读——表保留 A 中位列就是为了让读者看到
  基数大小。
- 归因用热轮口径；冷轮归因未做（冷带太宽，判据意义有限）。

## 9. 四段续（同日第四段）：分辨率压档 + trace 帧关联 + "不可分辨"结论复查

> 任务（用户）：继续下一轮；完成后用新遥测工具复查此前标记为"不可分辨"
> 的结论。复查对象从 CONCLUSIONS/benchmarks 里挖出三条约文。

### 9.1 W7：分辨率压档（地板 0.30% → 0.20%）

n=50 标定（同协议，h264-gpu）：均值 +0.006%、p50 0.122%、p95 0.867%、
sd 0.451%、SE 0.064% → |均值|+3×SE = **0.198%**。四轮标定序列
（09-13 / 本日三轮）：0.291 / 0.209 / 0.274 / **0.198**%——估计器稳定
且 n=50 已触 0.20，旧地板 0.30 开始**遮蔽真实分辨率**（masking）。
`PI15_FLOOR_PCT` 0.30 → **0.20**。边界（写进常量注释）：低于 0.30 的
建议值只在 n≥50 标定时出现（SE∝1/√n，n=25 的建议值仍多为 0.25~0.30）；
`PI15_LIMITS` 的 0.30 是默认门禁限（与默认 n=25 匹配）——要用 0.20 需
`--rounds 50` 标定 + 门禁 + `--std-limit` 三者一致。

### 9.2 W8：trace 帧关联（视图层，零引擎改动）

`_probe_trace_view.py`：空洞清单的每条空洞现在映射到**帧区间**（
`consume_feed` 每帧一条、按序对应帧号，空洞与哪些帧窗口相交即"第几帧
在等谁"的起点）；新增 `--frame K` 单帧视图（窗口内各线程活动按重叠
时长降序）。GPU 时间线验证：推理线程 163ms 空洞 = 帧 0-92（OCR 会话
启动期的真实等待）。

### 9.3 W9 复查一：C-47 TRT_DEFER_SYNC——**平价结论成立，机制首次直读**

前置补桩：defer 路径走 `submit/collect_argmax_deferred`，不经过 W2 打桩
的 `execute_device_argmax`——不补桩则 defer 臂 `trt_*` 全缺，结构转移
会被误读成残差膨胀。新增 `ocr.trt_submit`（异步提交）/`ocr.trt_collect`
（等上一批），与同步路径对位（relations 同列，每 run 只走其一）。

8 对交错（full 档，限 0.40/冷 3.40）：
- wall：**不可判定**（+0.167% ± 0.144%，符号 5/3）——C-47"e2e 平价"
  在 0.40% 新阈值下依然成立；
- 冷启：不可判定（−0.31% ± 0.93%）；
- **子相位直读**（此前只有探针间接证据）：同步路径 enq 0.244 + reduce
  （当场等）0.126 = 0.370s；defer 路径 submit 0.282 + collect（挪到下批
  等）0.085 = 0.367s——**等待确实被挪出（−32%），但 submit 变贵把收益
  吃回（+15%），净工作者时间持平**，且解码绑定下 OCR 端任何节省都到不了
  墙钟（C-49）。唯一强归因 `q_put_block_max −84%` 为 0.3ms 微小基数上的
  真实但无足轻重信号（基数列可辨）。
- **裁决：C-47 维持**（机制成立 + e2e 平价，双层都复核过）。

### 9.4 W9 复查二：C-10 GPU_PIPELINE_STREAM——**零收益结论成立 + 开销定位**

8 对交错：wall 不可判定（−0.094% ± 0.094%）、冷启不可判定。归因给出
比"零收益"更细的结论：`decode.stream_analyze` **+9.3%（7/8 `**`）**——
流水发射的逐 chunk 提交开销首次被正式 span 测到（基数 9.1ms/全片，约合
+0.8ms），被解码绑定吸收。**裁决：C-10 维持默认关**。

### 9.5 W9 复查三：优先级元实验——**ABOVE_NORMAL 不收窄噪声带**

`ab --aa --priority`（双臂 ABOVE_NORMAL，6 对）：热带 sd **0.530%** vs
无优先级基线 0.277%（n=6）/0.254%（n=8）——不窄反宽（n=6 的 sd 置信
区间宽，不构成"变差"的显著证明，但**肯定没有收窄收益**）。本机噪声
主因不是本进程被抢占（共享桌面的外部负载优先级不低于我们，提级无用）。
**裁决：不为门禁采用优先级**，`--priority` 维持 opt-in 诊断用途。

### 9.6 复查方法论备注

三条"不可分辨"结论全部**维持原判**——预期内：它们的原判定用的就是
配对差分口径，分辨率提升后"平价/零收益"应当更稳而不是翻转。这轮的
真实增量是**机制层**：平价从"墙钟没动"升级为"哪里动了、被什么吸收"
（defer 的等待转移、stream 的 span 开销）。翻转风险更高的其实是当年
用"符号乱"草草收案的条目（如 §19 优先级隔离的 av1 宿主 −3~−6%），那
需要产品补丁复活才能重测，不在本轮范围。

## 10. 五段续（同日第五段）：C-34 翻案——批量互补配对有真实收益

> 任务（用户）：继续调查别的可能翻案的历史结论。
> 方法：30 条 active 结论过筛（判定口径强度 × 前提漂移），候选排序后
> 对最强候选实测。**本轮出了一条真翻案。**

### 10.1 筛子与候选

三条候选：(a) **C-34 批量零增益**——前提"OCR-GPU-bound"被 C-48
（CPU OCR 2.1×）+ C-49（decode-bound）双重颠覆，复评触发"S5 内联后
解码格局变化"已发生；(b) C-04 并行数字（前提列自挂"待重测"）；(c)
§19 av1 优先级隔离（需复活已回退的产品补丁，缓）。C-34/C-04 同实验
出数，载体 = live 探针 `_probe_pool_pairing.py`。

### 10.2 首跑的假信号与三轮排查（先量后做的完整链）

首跑 5 轮：**均值只有 −2.2%（3/5 符号）**——若直接收案就是"零增益
维持"。但逐轮模式可疑：每轮**先跑的臂恒 ~34s、后跑的臂恒 ~27s**，
位置效应 ~26% 与预期效应同量级。三轮排查：

1. **背靠背四连**（全 seq 无间隔）：恒 34s → 推翻"闲置衰减/时钟"假设；
2. **NVML 采样判别**（交替模式复现）：时钟全 2685 热态、0 压频 → 排除
   时钟门禁类因素；且 **pair 恒 27s（含冷启 sm_min=375 的一次）、seq
   恒 34s**，与位置无关——首跑的"位置效应"解读本身是错的；
3. **INFO 日志分解**：seq 的逐视频墙钟（15.5/24.6/18.4s）远超单跑
   预期 → 发现两个被掩盖的事实。

### 10.3 两个被掩盖的事实（测量学 + 产品语义）

1. **探针的 "seq" 臂名不副实**：`pool.run(backends='nvdec')` 一律
   `max_workers=2`——"全 nvdec 顺序"实际是**双 NVDEC 会话并发**，正撞
   C-01 争用（hevc/av1 并发各退化 ~2.1×：11.7→24.0、14.1→26.5s）。
   探针对照臂从建轮起就带着这个语义错误。
2. **素材是全片**（7761/23970/23970 帧，非 bench 的 3000 帧窗）——
   单视频结论（3.1s）不可外推到批量口径。

补真串行对照（max_workers=1）：全 nvdec 33.7s ≈ 2w 34.0s（**并发零
贡献**，C-01 复证）；编码感知串行（h264→cpu）28.2s（**收益主体 =
派工**：h264→cpu 2.5s vs nvdec 7.9s）。

### 10.4 定稿证据（修复后的三臂探针，4 轮拉丁轮转）

| 臂 | wall（4 轮） | 中位 |
|---|---|---:|
| pair（编码感知+LPT×2w） | 26.98~27.39s | **27.03s** |
| nv2（全 nvdec×2w，原"seq"） | 33.85~34.06s | 34.03s |
| nv1（真串行） | 34.13~34.31s | 34.22s |

**pair −20.6%（vs nv2）/ −21.0%（vs nv1）**，4/4 轮零异常、三臂段数+
唯一文本集逐位一致。pair 27.0s ≈ makespan 地板（2.5s cpu 项与 nvdec
重叠 + 11.7 + 14.1s）——已接近该素材的调度上限。

**翻案机制**：旧判（2026-09-10）的零增益成立于 OCR-GPU-bound——当时
TRT 是并发共享瓶颈，解码侧怎么派工都被 OCR 串行化吸收；C-48 把 CPU
OCR 提速 2.1×、C-49 确认 decode-bound 后，解码派工首次成为批量的
决定性杠杆。前提一变，结论翻转——这正是铁律 9（迁移清扫）设计要抓
的情形，本轮由主动复查补上。

### 10.5 落地

- **C-52 新增**（批量互补 −20.6%/−21.0%，收益主体=编码感知派工，
  并发零贡献；边界：素材全 hevc/av1 时 pair 退化为 nv2）；**C-34 →
  superseded**；C-04 前提行"并行待重测"销案（指向 C-52）。
- **探针修复**（live 修复义务）：三臂制（补真串行基线）+ 拉丁轮转 +
  **瞬态异常防护**（臂内偏离自身中位 >10% 的轮剔除出判据、原始值
  保留可见——首跑 2 次 pair=34s 环境瞬态曾把 −20% 的真效应拖成 −2.2%
  的假"零增益"，教训与 §8.1 同源：判据必须对环境瞬态免疫）+ 逐视频
  wall/backend 落盘。
- 边界备注：批量收益依赖素材含 h264（可卸载到 CPU 臂）；全 hevc/av1
  素材 pair=nv2 无收益。NVDEC 双会话争用依旧（C-01），pair 的价值在
  于把 h264 从 NVDEC 挪走而非"并发更快"。

## 11. 六段续（同日第六段）：fork 遥测穿透 + hybrid 引擎级重测

> 任务（用户）：改善 decord fork 侧遥测，遥测数据穿透到引擎层（一次
> extract 拿全部信息），然后重新测量 hybrid 性能。

### 11.1 fork 侧（/d/Repo/decord，5 处改动）

1. `ThreadedDecoderInterface::HybridStatsProbe()` 虚方法（缺省空串）；
   `HybridThreadedDecoder` 覆盖——**"k=v;..." 线协议**输出全部计数器
   （与 stderr 汇总同源，原子快照、任意时刻可调）。
2. **新增 HOL 直方图**：episode 入口对侧存货的 log2 桶 ×24（per-episode
   一次数组写，默认累计零成本）——sum/max 之外补形状，对齐引擎 W1 口径。
3. **新增份额轨迹**：`DECORD_HYBRID_TRACE=1` 时速率就绪后的每个派工决策
   记 [t_us, share_bp, rc, rg]（首 1024 点；泵的适应过程自此有轨迹）。
4. `VideoReader::HybridStats()` + C API `_CAPI_VideoReaderHybridStats` +
   python `vr.hybrid_stats()`（trace 与 kv 分隔符冲突用 partition 规避；
   非 hybrid 返回 None）。
5. 旧 `DECORD_HYBRID_STATS` stderr 块原样保留（探针兼容）。

**本机重建记**（构建环境已漂移，留档防再踩）：① git-bash 直调 .bat 的
引号转义会废掉 vcvars——**落盘 .bat 经 cmd 跑**；② MSVC 升级后旧
build 目录的 CMake 缓存指向已删编译器，须清缓存重配；③ **ffmpeg SDK
目录消失**——用 ffmpeg-9.0 源码树内构建产物组装 `ffmpeg-9.0-sdk/`
（lib/include 按模块子目录 + bin DLL）；libavdevice 未构建且 decord
不引用（`DECORD_USE_LIBAVDEVICE` 未定义）→ **空 def 造 import lib 占位**；
④ `-DUSE_CUDA=ON`（零 Toolkit：驱动 API 动态加载，**导入表无 nvcuvid
是正常的**——验证要看 .obj 而不是依赖表）；⑤ site-packages 替换
decord.dll + video_reader.py（.bak-083 备份可回滚）。

### 11.2 引擎侧（report v6）

两后端在 `vr.close()` 前调 `vr.hybrid_stats()`（getattr 缺席容忍，
stock decord/非 hybrid → None），经 RunResult.fork_stats →
`report["hybrid"]`（v6 只加键；缺席≠空值）。端到端验证：一次 hybrid
extract 的 RunReport 自带 33 键 fork 快照。

### 11.3 hybrid 引擎级重测（新协议：时钟门禁/冷热分离/归因/自动判定）

**⚠ 口径纠正（用户指出，已被数据证实）**：hybrid 的评估基线必须是
**目标编码的最快纯解码臂**——h264 是 CPU（~2000fps vs NVDEC ~990fps），
不是 NVDEC。首轮拿 nvdec 当 h264 基线（−46.5%）高估了 hybrid；正确
口径下 h264 hybrid **+53.6% 慢**。此规则已进 rules.yaml/AGENTS/结论，
防再遗忘。

| 码 | 窗口 | 最快纯臂（基线） | 基线 wall | hybrid wall | 判定 |
|---|---:|---|---:|---:|---|
| h264 | 3000 | **CPU**（cpu+TRT） | 1.102s | 1.688s | **B 慢 +53.6%（8/0，SE 1.7%）**；冷启 +41.4% |
| hevc | 23970 全片 | NVDEC | ~12.0s | 9.21s | **B 快 −23.0%（6/6，SE 0.43%）**；冷启 −21.4% |
| av1 | 23970 全片 | NVDEC | ~14.4s | 9.53s | **B 快 −33.8%（6/6，SE 0.55%）**；冷启 −34.2% |

**结论：h264 选显式 cpu；hevc/av1 选 hybrid。** 机制（fork 直通）：
h264 hybrid 的 CPU 臂被混跑干扰+HOL 保序税拖到 1218fps（单跑 ~2000），
而 GPU 臂（~990fps）是弱腿——加弱臂不如不加；hevc/av1 正相反，CPU 臂
是净增量（hevc CPU 臂 822fps 使 GPU 从 75% 帧分走 25%；av1 CPU 份额
25→33% 随时间爬升——泵适应轨迹首次可见）。与 C-52 批量配对
（h264→cpu）完全自洽。

窗口选择的教训：hevc 用 window=3000（bench 默认）时 hybrid **+94% 慢**
——正落在 C-40 交叉点（1500~3000 帧）内侧；换全片后翻为 −23%。
**hybrid 评估必须越过目标码的交叉点窗口。**

fork 直通机制读数（每 run 自带）：
- h264：CPU 份额 72→76%，CPU 臂 1218fps（混跑税 −39% vs 单跑）；
- hevc：GPU 主导（18000/23970 帧），CPU 臂 822fps 慢腿，HOL GPU 侧
  757 episodes（快臂让路结构）；cache_peak 189MB（512 预算内）；
- av1：CPU 份额 25→33% 爬升（share_bp 2535→3349）。

**口径澄清（防误读）**：C-46 的"引擎 A/B hevc −6.29%/h264 −4.78%"经
本轮核对是 **fork 新机制 vs 旧机制**的 A/B（定稿协议明确被测对象=发布
wheel 的 fps_b 达成率），不是 hybrid vs 纯解码臂的 e2e 对比；两者长期
在转述中混用。本轮给出后者（上表）。

### 11.4 测量事故两笔（如实记录）

1. **并发污染**（本轮自伤）：hc 首轮 A/B 期间 PI-15 标定在后台同时跑
   （801MB 进程持续做 extract），hybrid 臂被压到 2.76s（vs 干净态
   1.69s）；同时 PI-15 自己的 A/A 也被污染（sd 5.28%、|Δ|max 19.3%，
   史上最差）——**双向污染**。教训重申：标定期机器独占，后台任务与
   前台实验不得重叠（此前 n=50 轮遵守了，本轮装载文档时疏忽）。
   PI-15 须干净重标（见 §11.6）。
2. **附带修复**：`bench ab` 判定输出"B 显著快"分支的格式串缺参（5 个
   占位 4 个实参）——hybrid 三码全部触发该分支时首次暴露，TypeError
   崩在打印上（数据已入 registry）。修复 + 补参数。另：逐对 |Δ|>20%
   的"环境异常"警示在**真实大幅效应**（如 +53.6%）下会整表误报——
   它设计时假设效应 <20%，读表时须与判定行联读。

### 11.5 口径规则的固化（用户纠正的落点）

"hybrid 对照臂 = 目标码最快纯解码臂"已写入三处规范层 + 个人记忆：
rules.yaml A/B 条款注释（执行体入口，最先被看到）、AGENTS hybrid 条目
（注入核）、C-53 前提列（含"曾错用 nvdec 基线"的翻案痕迹）。错误基线
的 −46.5% 数据保留在 benchmarks.yaml（`wrongcaliber` 键）——错误口径
的数字不删，标记后留档。

### 11.6 PI-15 干净重标（v6 遥测面）

被 §11.4 污染的标定作废；独占机器重跑（n=25/inner=3）：均值 −0.055%、
SE 0.080%、p95 0.772% → \|均值\|+3×SE = 0.295% → **阈值维持 std
+0.30% / full +1.20%**。五轮标定序列（本日）：0.291/0.209/0.274/
0.198(n=50)/0.295——新增遥测面（fork 直通 + v6 段）未推高噪声带；
地板 0.20 仍仅在 n=50 协议下可达。



## 复评触发

- 换卡/驱动（时钟门禁 k=0.75 依 3105/2700 平台比）；换机器（全部标定重跑）
- bench 协议再改（--aa 重标）；trace 转正评估（交错 A/B 级成本证据）
- `nvmlDeviceGetMaxClockInfo` 语义变化（boost 上限 vs 实际平台）

## 证据

- 探针：`_probe_cycle_quant.py`、`_probe_clock_gate.py`、`_probe_trace_view.py`
- 产物：`bench/cycle_quant.json`、`bench/clock_gate.json`、
  `bench/trace_test5_cpu.json`、`bench/trace_test5_nvdec_gpu.json`
- 验证轮：`bench/registry.jsonl` 的 `p2-verify-cpu`/`p2-verify-gpu`/
  `p1-smoke`（gpu 字段样例）
- 相关结论：C-42（占比≠可回收）、C-49（绑定实验口径）；上午日志 §7-§8
