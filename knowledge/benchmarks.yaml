# 权威性能数字（S0 基线 + S6 轮，2026-09-10；机器：7945HX + RTX 4060 Laptop
# 8GB，driver 610.88，decord fork 0.8.2，TRT sm89；3000 帧窗口热轮中位）
# 事实源：S0 = tests/golden/bench_baseline.json；S6 起 = bench/registry.jsonl
# （append-only RunReport）。本表为人工确认的权威口径。
- metric: e2e_wall_3000
  h264-gpu-nvdec-trt: 3.107
  h264-cpu-cpu-trt: 1.131
  hevc-nvdec-trt: 1.528
  av1-nvdec-trt: 1.814
  noise_floor_warm_pct: 1.3
- metric: decode_ceiling_fps
  h264_nvdec: 1013
  h264_cpu: 3300
  hevc_nvdec: 2127
  av1_nvdec: 1780
- metric: host_gpu_equivalence
  window: [0, 2000]
  segments: bitwise
  texts: bitwise
  conf_median_delta: 0.0
  conf_worst_delta: 0.00454
# ── S6 轮（交错 A/B：A/B/A/B 独立子进程 + 逐对符号一致性）──────────────
- metric: s6_measurement_env
  same_code_two_runs_delta_pct: 7.7    # 同码连跑两次最大散布 → A/B 必须交错
  gpu_thermal_slowdown_cumulative_s: 87.7
  method: tools/bench.py ab
- metric: s6_telemetry_overhead_pct    # PI-15（3000 帧；交错 5 轮 × 每档热轮 2 次）
  off_s: 3.1260
  std_s: 3.1296
  std_vs_off_pct: 0.115
  full_vs_off_pct: 0.038
  verdict: std≈full（full<std 证明残差在噪声内）；0.1% 阈值低于本机分辨力（±0.1%）
- metric: s6_trt_profile_batch         # S6-g′：TRT profile batch 6→18
  micro_1116img_b6_s: 0.663
  micro_1116img_b18_s: 0.557
  micro_delta_pct: -16.0
  e2e_h264cpu_hot_delta_pct: -5.85      # 交错 3 对，符号 3/3
  e2e_ocr_infer_delta_pct: -33
  sub_chunks: 181 -> 61
  texts: bitwise                        # 段结构/文本 sha 逐位不变；置信度第 4 位漂移
  engine_build_first_run_s: 68
- metric: s6_coldstart_3000            # h264-cpu 冷/热相位
  cold_wall_s: 1.799
  hot_wall_s: 1.215
  cold_engine_init_s: 0.410
  cold_calibrate_s: 0.501            # 含 NVRTC 首编 0.4s（cubin 缓存后 0.09s）
  cuda_core_import_s: 0.224
  trt_deserialize_s: 0.39
- metric: s6_warmup_1500               # 显式预热（h264-cpu）
  warmup_s: 0.500
  first_extract_after_warmup_s: 0.798
  engine_init_after_warmup_s: 0.00007
  first_video_delta_pct: -33
- metric: s6_consumer_submission       # 交错 A/B 冷启动（各 3 对）
  reduce_d2h_sync_to_async_h264cpu_pct: -5.19
  keep_crops_d2h_batched_h264gpu_pct: -2.45
  pi14_gpu_ctc_bitwise: pass
- metric: s6_nv12_lut                 # S6-f′：宿主路径 Y 展开 LUT（逐位等价）
  host_path_3000f_yuv_decode_luma_batch_s: 0.090 -> 0.052
  phase_delta_pct: -42
  wall_3000f_s: 1.168 -> 1.135
  bitwise: pass                       # 256 值全枚举 + 批量/单帧/full-range
- metric: s6_ocr_fill
  fill_pct: 40.2
  pad_cols: 224
  content_cols_avg: 90
  pad_224_to_320_hot_pct: 3.66
- metric: s6_reorder_window_3000       # 交错 A/B 4 对，符号 4/4 一致
  rw1_vs_rw64_hot_pct: -5.05
  rw1_vs_rw256_hot_pct: -11.16
  adaptive_rule: roi_aspect <= floor/OCR_TARGET_H -> window=1
# ── hybrid 差距分解（S6 续轮，2026-09-11；详见 docs/log/2026-09-11-hybrid差距分解.md）──
- metric: hybrid_decode_capability      # 纯解码 fps（ROI/gray/批 64/3000 帧，fork dll）
  h264: {cpu: 3573, nvdec: 983, hybrid_host: 3783, hybrid_gpu: 3052, ideal_parallel_sum: 4556}
  hevc: {cpu: 754, nvdec: 2036, hybrid_host: 1833, hybrid_gpu: 2116, ideal_parallel_sum: 2790}
  av1:  {cpu: 1112, nvdec: 1694, hybrid_host: 2686, hybrid_gpu: 2174, ideal_parallel_sum: 2806}
- metric: hybrid_engine_gap_pct          # 引擎吃掉多少（L1 hybrid_gpu → L3 热轮墙钟）
  h264: 18
  hevc: 10-27
  av1: 0
- metric: hybrid_routing_3000            # 出厂 dll 实测最优路由（引擎热轮中位 s）
  h264: {best: cpu, best_s: 0.994, hybrid_s: 1.384, nvdec_s: 3.107}
  hevc: {best: nvdec, best_s: 1.528, hybrid_s: 1.577, cpu_s: 4.249}
  av1:  {best: hybrid, best_s: 1.372, nvdec_s: 1.818, cpu_s: 2.898}
  auto_policy: NVDEC 优先（C-08 用户决策，未改）
- metric: fork_dll_unpublished_value   # DECORD_LIBRARY_PATH=build-081fix vs stock 0.8.2
  h264_hybrid_engine_pct: -12.73
  av1_hybrid_engine_pct: -11.19
  h264_hybrid_gpu_decode_pct: 8.2
  hevc_hybrid_gpu_decode_pct: -8.4      # fork 回退：发布前需复核 hevc
- metric: hybrid_cpu_threads_policy     # 交错 A/B（3000 帧热轮）
  shipped_12_to_16_h264_pct: -6.49
  shipped_12_to_16_hevc_pct: -12.72
  fork_12_to_16_h264_pct: -3.91
  new_default: cores//2 clamped [8,16]
- metric: hybrid_engine_loss_refuted    # 引擎侧损耗的两条否定结果（别重复试）
  buffer_size_128_to_512_h264_pct: 0.51
  buffer_size_128_to_512_hevc_pct: 2.00
  gpu_pipeline_stream_0_to_1_h264_pct: 0.71
  verdict: 队列/预取均无收益；剩余候选=CPU 争用（待线程时间线剖析）
# ── §8.6 r5 资源层首批真读数（L1 差分 / L2 NVML，2026-09-11）──────────────
- metric: resource_layer_call_cost      # 单次采样成本实测（本机 Windows）
  time_process_time_us: 0.05
  kernel32_io_counters_us: 2.5
  psapi_memory_info_us: 4.6
  cudaMemGetInfo_us: 0.9
  nvml_GetUtilizationRates_us: 0.3
  nvml_GetMemoryInfo_us: 1.7
  nvml_GetDecoderUtilization_us: 0.5
  banned_psutil_threads_ms: 43.0        # 地雷：一次就吃满 PI-15 预算
  banned_psutil_num_threads_ms: 5.1
  l1_checkpoint_total_us: 10            # 每 run 仅 4 个边界 ≈ 40µs
- metric: per_phase_cores_h264cpu_3000  # h264-cpu（TRT OCR），热轮差分
  calibrate: {wall_s: 0.065, cores_avg: 9.18, threads: 4, rss_delta_mib: 267.4, disk_read_mbps: 967.2}
  decode:    {wall_s: 0.953, cores_avg: 21.21, threads: 4, rss_delta_mib: 71.5, disk_read_mbps: 108.6, vram_used_mib: 1667.5}
  ocr_tail:  {wall_s: 0.068, cores_avg: 7.38, threads: 2, rss_delta_mib: -349.9}
  reading: CPU 软解相位实测用到 ~21/32 逻辑核 → hybrid 的 CPU 分片必与引擎线程争核
- metric: nvml_sanity_check             # 探针不可自嗨：CPU 软解配置下 NVDEC 必须读 0
  config: h264-cpu
  nvdec_util_pct: {min: 0, p50: 0, max: 0}
  gpu_util_pct: {min: 7, p50: 7, max: 34}
- metric: pi15_gate_calibration          # 门禁阈值按本机 A/A 现测标定（r11 用户授权放松）
  bad_estimator_same_code_two_reads_pct: [-0.504, 0.930]   # 旧判据：三档各自中位相减
  fixed_order_full_delta_pct: 3.32       # 位置效应：每轮先跑的档系统性偏慢
  subproc_aa_pct: {p50abs: 0.24, p95abs: 2.33, maxabs: 3.98}   # 子进程模式噪声
  inproc_aa_25pairs_inner3_pct: {mean: -0.179, median: -0.365, sd: 1.053, se: 0.211,
                                 p95abs: 1.811, maxabs: 2.597}
  rule: limit = ceil_0.05(|A/A 均值偏差| + 3×SE), floor 0.30%
  calibrated_limits_pct: {std: 0.85, full: 0.85}
  statistic: 配对差分**均值**（轮转后差值 ±X 双峰，中位数落在峰上→失效）
  sign_majority: 0.70
- metric: pi15_final_verdict_quiet      # 独占机器复测（25 对 × inner 3，h264-cpu/3000）
  off_s: 1.2106
  std_s: 1.2096
  full_s: 1.2053
  std_vs_off_pct: 0.029
  std_se_pct: 0.291
  full_vs_off_pct: 0.140
  full_se_pct: 0.257
  signs: {std: 13/25, full: 13/25}
  verdict: 通过（点估计其实优于原设计目标 +0.1%/+1%，但 SE 0.29% 不足以证明）
- metric: telemetry_cost_guard          # 严格性搬到的确定性守卫（µs 级分辨力）
  real_run_records: {span_keys: 9, span_samples: 138, counter_keys: 10, gauge_keys: 8, checkpoints: 4}
  std_full_path_budget_ms_per_run: 2.0
  off_tier_records_cost_ms_per_run: 0.012
  report_assembly_ms: {samples_138: 0.107, samples_552: 0.078, samples_2760: 0.072}
  note: 组装成本与样本数无关（drain 一次合并）；99ms 那种数字是 environment() 首调
- metric: gpu_fingerprint_source        # 环境指纹改走 NVML（进程级缓存，落在报告路径上）
  nvidia_smi_subprocess_ms: 43.1
  nvml_first_call_ms: 20.8
  nvml_calls_us: {GetUtilizationRates: 0.3, GetMemoryInfo: 1.7, GetDecoderUtilization: 0.5}
  fallback: nvidia-smi（记 gpu_source=nvidia-smi；两者皆失败记 unavailable）
# ── hybrid 混跑损耗（fork 死锁修复后才可测；2026-09-11，600 帧/臂）──────────
- metric: hybrid_mixing_loss_fps        # FORCE_SIDE 三臂（短窗含暖机，看相对量）
  h264: {gpu_arm: 920, cpu_arm: 2200, mixed: 1588, ideal_sum: 3120}
  hevc: {gpu_arm: 1976, cpu_arm: 898, mixed: 1334, ideal_sum: 2874}
  mixed_vs_best_single_pct: {h264: -28, hevc: -32}
  reading: 混跑相对**最优单侧**是净亏损（与"hybrid_gpu 仅达理想 67–77%"是两笔账）
  first_suspect: 队头阻塞——发射按 chunk 严格保序，chunk 边界=关键帧（GOP≈299 帧）
  fork_bug_fixed: 99b8785（bootstrap/BuildPlan 绕过 FORCE_SIDE → force=cpu 死锁，
                    实测 637 帧 CPU 存货卡在一个永不落地的 GPU chunk 后面）
  unforced_recheck_fps_2000: {before: [2980, 2994, 3071], after: [2833, 3077, 3041]}
  golden_with_fork_dll: 28/28 逐位一致
# ── CPU 速率估计器"排空棘轮"缺陷 + sustained 修正（§7.2-1 先量后做）────────
- metric: hybrid_cpu_rate_ratchet         # 非打印 DECORD_CPU_RATE_DEBUG trace 锤实
  hevc_hybrid_fps: {ewma: 1951, sustained: 2688}   # decode-only 3000f×3 中位
  h264_hybrid_fps: {ewma: 3537, sustained: 3167}   # sustained 略降但 h264 走显式 cpu
  av1_hybrid_fps: {ewma: 2645, sustained: 2580}    # ≈持平（噪声内）
  root_cause: 容量 EWMA（升1.3x/降5%）被**解码器预跑存货排空孤峰**逐发棘轮
  trace_evidence: hevc 真产率 r≈630-900 但 r=2491..9138 孤峰每~15折一发，
                  EWMA 永久卡 1200-2500（纯 nvdec 单臂仅 2036）
  consequence: BuildPlan 冻结高估 rc → hevc CPU 份额翻 2-3x → 混跑 1568 < nvdec 2036
  sustained_fix: 滑窗8折内连续4折最小值的最大值 + 0.997/折慢降锁
  key_correction: sustained 下 hevc hybrid 2688-2778 **> 纯 nvdec 2036**，达并行
                  理想(2790) 的 96-99% → §7.1 "混跑净亏损"很大程度是估计器伪影
  gpu_out_deadlock: hybrid_gpu（引擎 GPU 管线口径）真实倾斜计划暴露先存在死锁
                    ~2/8 挂死（EWMA 对照 16/16 干净）→ sustained 暂 opt-in
                    DECORD_CPU_RATE_SUSTAINED=1，默认仍 EWMA（金标 28/28）
  fork_commit: 75b8602
# ── 2026-09-12 后续：kick 突发治死锁 + sustained 转默认（fork 4ccf889）──────
- metric: hybrid_sustained_default          # 死锁根因 = open-GOP DPB 扣 IDR+leading
  deadlock_root_cause: 跨侧切换后离场侧等不到后续包，kick 单 IDR 冲不开 open-GOP
                       DPB（挂死现场扣 6 帧 = IDR+5 leading，side_pending=6 吻合）
                       → 瞬态滞留变永久冻结 → 队头缺帧/盲窗被对侧库存吃满
  fix: 跨侧切换后新 chunk 前 KICK_BURST=5 包克隆喂离场侧（越界 stash/陈旧
       丢弃兜底）；DECORD_HYBRID_KICK_BURST=0 可消融回单包 kick
  stress: 引擎 GPU 管线全片判别：DECORD_HYBRID_KICK_BURST=0 → hevc 4/4 挂死，
          =5 数十次运行零挂死（金标 28/28 含 hybrid）。⚠️ 纯 VideoReader 慢
          消费者 harness（3000f 与全片）复现不了该死锁（kick=0 也干净）——
          §8.2"复现配方（无需引擎）"记载有误，判别必须走引擎
  gate_off: sustained 转默认 + DECORD_CPU_RATE_SUSTAINED env 删除（双轨期结束）
  e2e_full_video: # 引擎 GPU 管线全片 e2e，配对 3 遍，段数逐位一致（_probe_e2e_mode）
    hevc: {ewma_s: 12.73, sustained_s: 9.66, delta: "-24%，3/3"}
    hevc_vs_nvdec: {nvdec_s: 12.44, hybrid_s: 9.60, delta: "-23%，2/2 —— hevc
                    hybrid 首次显著转正（旧口径 ≈ 最优单侧 ±3-5%）"}
    h264: {ewma_s: 3.30, sustained_s: 3.15, delta: "-4.7%，3/3"}
    av1: {delta: "持平（±1% 漂移内）"}
  decode_only: hevc hybrid 2498 / hybrid_gpu 2659 fps（并行理想 2790 的 90-95%）
  golden_with_fork_dll: 28/28 逐位一致
  leftover: 上传批次碎裂已定性为**无害**（见下条 kUploadBatch A/B）
  fork_commit: 4ccf889
  dev_dll_md5: 52a10789
# ── 2026-09-12 续：kUploadBatch A/B 负面结果（§9.4-1 关账）─────────────────
- metric: hybrid_upload_batch_ab          # DECORD_HYBRID_UPLOAD_BATCH 消融（fork 281a738）
  result: 批大小不是杠杆，上传链重构不做
  mechanism: 有效批均 1.1-3 的约束是 **cpu-empty**（上传者消费快于 CPU 解码者
             补充，队列近恒空），不是上限——32/64 档批均纹丝不动（恒 1.1）
  batch1_arm: 逐帧同步旧行为（batch=1）引擎全片 e2e 无差：hevc 配对 3 遍中位
              9.54 vs 9.66s（符号混乱=漂移内）；av1 9.55s、h264 3.28s 均在
              多rep基线带内 → 上传同步成本完全不在关键路径
  conclusion: cpu-empty 提前冲刷（低发射延迟）是正确默认；avg_batch 1.1 vs 设计
              8 的"账面差距"无性能后果，勿再立项重构
  knob: DECORD_HYBRID_UPLOAD_BATCH 保留（操作点变化时可复测；1=逐帧同步）
  fork_commit: 281a738
  dev_dll_md5: 5ffacd49

# ── 2026-09-12 续：全片 vs 3000f 窗口的 HOL 摊薄实验（§11）─────────────────
- metric: hybrid_hol_fullvideo            # _probe_hol_stats --frames 99999，双 ctx × 2 reps
  question: 队头阻塞消耗能否被完整视频时长摊薄？
  answer: 只摊薄宿主路径；HOL 秒数随片长增长（固有等待），跨时长健康指标 =
          吞吐达成率而非 HOL%
  host_ctx: hybrid(宿主) 三码全片 +9~27%：h264 3135→3975（达成 69%→87%）、
            hevc 2498→2897、av1 2445→2670（→95%）；hevc 全片超 3000f 理想分母
            （单臂短窗口径低估长跑，非超并行理想）
  gpu_ctx: hybrid_gpu 分化——hevc +7.8%（→103%）；h264 零摊薄（68% 持平）；
           **av1 回退 6.4%**（2462→2304，88%→82%）
  av1_regression_mechanism: sustained rc 长片塌到 702（单臂真值 1112 的 63%）
           → 计划 78% 倾斜 GPU → g-head strand 17884 / HOL 6.52s；引擎级未显形
           （消费节奏慢自然配平，e2e 与 EWMA 持平），快消费场景（批量互补/
           并发流水）会踩到
  hol_metric_note: 全片 HOL% 普升至 32-68% 且 g-head 绝对值 4.3-6.5s，但吞吐
           同向上升——HOL 时间 = 快臂份额跑完后按保序等慢臂的固有等待，
           不可由消费端调度回收
  next: av1/h264 sustained 读数钳制或分相位采样（§11.2 排序第一）
  fork_commit: 281a738
# ── 2026-09-12 续：控制变量重测——test6 转 h264 + 全片单臂（§12）────────────
- metric: hybrid_controlled_codec_matrix  # test6_h264.mp4 = libx264 crf19 medium，
  #  High profile、严格 GOP 300（-sc_threshold 0）、1080p60、23970 帧、同 ROI
  first_confounder: 编码——同内容 CPU 单臂 h264/av1/hevc = 2690/1290/964（2.8x）；
             旧"h264 67% 最远"有 25% 是分母错（test5 CPU 3573 ≠ test6 内容真值 2690）
  arm_longrun_gain: CPU 单臂全片 vs 3000f = hevc +28%、av1 +16%；NVDEC ≈0%
             （固定功能不挑内容不挑片长）→ 旧理想和系统性低估 5-15%
  corrected_matrix: # 全片 23970 帧，达成率 = fps/(单臂和)
    h264lg: {cpu: 2690, nvdec: 965, ideal: 3655, hybrid: 3268, hybrid_gpu: 2944}
    hevc: {cpu: 964, nvdec: 2113, ideal: 3077, hybrid: 2897, hybrid_gpu: 2866}
    av1: {cpu: 1290, nvdec: 1734, ideal: 3024, hybrid: 2670, hybrid_gpu: 2304}
  achievement: hybrid 89/94/88%，hybrid_gpu 81/93/76%（h264lg/hevc/av1）
  dev_host_gap_law: 设备路径差距与 CPU 臂产能正相关（hevc 1.1%/h264 9.9%/
             av1 13.7%）——快 CPU 编码下上载交付上限 bind（h264lg c-head
             3.45s、strandmax 仅 440/571 = 交付跟不上而非保序积压）
  av1_76pct_mechanism: BuildPlan 一次性冻结在首窗坏读数（全片仅 6 折样本、
             rc=702 = 真值 1290 的 54%）；慢降锁保读数不棘轮但规划不重读
  supersede: §11 表的达成率全部按本条分母重算（hevc"超100%"为分母伪影）
  next: ① 首窗读数钳制/分相位采样（修 BuildPlan 输入质量）② 设备路径上载
        天花板量化 ③ 0.8.3 wheel 发布
  fork_commit: 281a738
# ── 2026-09-12 续：replan（重规划）实验——机制否定 + av1 归因翻案（§13）──────
- metric: hybrid_replan_ab                # 实现于 fork 工作区，3码×3对交错全片 A/B 后被否
  question: §12.2 假设"av1 76% = 一次性规划冻结在首窗坏读数"——重规划能否修复？
  result: 否——读数正确稳定，replan 永不触发；机制回滚未进 fork 历史
  evidence: rc 在全部 125 折样本上稳定 702-875（偏离冻结值 <8pp）；replans=0
            于 6/6 cell；A/B Δ-1.5%~-0.1%（噪声带）
  real_mechanism: CPU 软解臂混跑期真实折价——纯臂 1290 → 混跑期 sustained
            读数 ~760（-41%，dav1d 多线程与喂包/消费/上载抢核）；GPU 臂不折价。
            按混跑理想（折价 rc + 纯 nvdec）重算：av1 91-101% / h264lg 91-96% /
            hevc 96-98% → 调度器已达自身上限，剩余差距 = CPU 供给折价
  closes: 首窗钳制 / 分相位采样 / 重规划整族思路（估计器输入无问题）
  next: CPU 臂折价的供给侧（线程数/喂包隔离）——h264/av1 引擎走显式路由已绕行，低优先
# ── 2026-09-12 续：hybrid CPU 臂线程定档（§14）——错配伪影修正 + 引擎统一 codec 感知 ──
- metric: hybrid_cpu_threads_tier
  question: §13.3 的"CPU 臂混跑折价 av1 −41%"是否真实？hybrid CPU 臂线程
            档位（旧：逻辑核//2 钳 [8,16]=本机 16）是否最优？
  result: 折价是口径伪影——探针 hybrid_gpu 传 num_threads=0 在 decord 内
          隐式落 16T（hw//2 钳 [2,16]），单臂 cpu 跑 32T，"−41%"= 16T 混跑
          vs 32T 纯臂；同 nt 诚实折价仅 −7~−23%（随 nt 升高加深）。引擎
          e2e 配对 A/B（文本集逐位一致）：16→24 h264lg −5.9%/hevc −4.4%/
          av1 −12.8%（全 3/3）；24→32 h264lg 再 −4.8%（3/3）、av1 反向
          +5.1%（dav1d 过订阅）。档位顶点 h264/hevc=32、av1=24 恰为
          _decode_num_threads 现行策略 → hybrid 分支统一复用，删除 //2 档。
  caveat: decode-only 24T 已饱和（32T 无增益）但引擎口径 32T 更优——消费
          节奏（OCR/分段线程共存）改变顶点，档位判定以引擎 e2e 为准；
          旧 §11.3"av1 长片回退 6.4%"/§12.2"76%"均为 16T 伪影
  evidence: tools/_probe_hol_stats.py --nts 16,24,32 全片矩阵（纯臂/hybrid/
          hybrid_gpu 三 ctx）；tools/_probe_hybrid_threads_e2e.py 配对 A/B；
          压测 32T 18/18 零挂死；金标重录后 28/28（C-h264 三例系 §12
          重转码改 sha 致预存失配，非线程改动）
  next: 0.8.3 wheel 发布（口径不变）；上载天花板量化（低优先）
  fork_commit: 281a738（fork 零改动，本轮全部引擎侧）
# ── 2026-09-12 续：0.8.3 wheel 发布验证轮（§15）────────────────────────────
- metric: decord_083_wheel_validation
  question: fork 九提交打包的 0.8.3 wheel（cp313 本地构建）行为是否与已验证的
            dev dll 一致？发布门禁（金标 + kick 判别）是否通过？
  result: wheel≡dev 全对位——金标 28/28 ×2（重录整矩阵同序后；C-h264-cpu
          曾 27/28 系局部 --case 重录录在冷池态、全量 verify 跑到时已热，
          F-8 刀锋段置信度末位翻转，非回归）；e2e 三码全片×2 段数一致；
          压测 6/6；decode-only av1@24T 2753 vs dev 2782fps（±1%）
  caveat: kick=0 消融判别（§10.2 的 4/4 挂死）当日对 wheel 与 dev dll 均不可
          复现（各 4 次全干净，16T/32T 都试）——死锁窗口时序敏感；替换证据
          = 默认运行 kicks c=30 g=30 + wheel≡dev 对位。历史 4/4 对照保留
  next: 用户执行：fork push origin master → GitHub dispatch release.yml
        (version=0.8.3)；CI 产物发布后抽验（pip 装 + §15.4 矩阵复跑）
  fork_commit: 281a738（零新提交；wheel sha256 bad116d0…）
# ── 2026-09-12 续：宿主路径环死锁修复 + 全面反超判定矩阵（§16）────────────
- metric: hybrid_host_deadlock_fix
  question: hybrid+ONNX（宿主管线慢消费）挂死根因与修复
  result: 两层致死环——①kick 突发递送被 NeedsPackets 在途窗口截断（ONNX
          慢消费顶满 inflight→demux 停读→剩余克隆不来→队头永冻）；
          ②KICK_BURST=5 冲不开深重排流（libx264 medium B 金字塔，5 包喂入
          后 NVDEC 仍扣 5 帧）。修复（fork 02c91e6）：突发武装期
          NeedsPackets 无条件放行 + KICK_BURST 5→16；消融坐实（只修①
          仍挂；②=16 后 4/4 干净）
  evidence: pop-stall 签名（kick=0/1、cud=0/5/0、em=293/300、np=0）；
          TRT/压测全绿回归（金标 28/28、压测 9/9）；wheel sha16 1b180227
  next: 无（关闭；发布随 0.8.3）
  fork_commit: 02c91e6
- metric: hybrid_full_matrix_verdict
  question: 三码 × 两输出路径，hybrid 是否全面反超最优单解码器？
  result: 设备路径（TRT）三码全面反超（h264lg −11.8% / hevc −19.5% /
          av1 −32.8% vs 最优单侧 e2e）；宿主路径（ONNX）不反超——h264lg
          三者持平（OCR 瓶颈）、hevc 慢 11%、av1 慢 31%（nvdec 结构性
          占优：CPU 臂与 ONNX 抢核）；decode-only 6 格仍全反超（+27~60%）
  caveat: 宿主路径结论前提 = ONNX 吃满核；换更轻 OCR 或未来 ORT 提速需复评
  next: 文档警示（README/AGENTS 已注）；无代码动作
  fork_commit: 02c91e6（测量时点）
# ── 2026-09-12 续：kick 清偿机制定稿——反馈式取代定长（§17）────────────────
- metric: hybrid_kick_feedback
  question: §16 的 KICK_BURST=16 仍是猜深度调参（重排 >16 的流仍挂）——机制级修复？
  result: 反馈式清偿（fork b67f9eb）：切换时刻快照离场侧债务
          （side_pending_），此后非 key 包持续克隆至 pending−cloned ≤ 0
          （发射/陈旧丢弃都减 pending——由解码器实际产出闭环，零深度假设）；
          NeedsPackets 债务放行（治 §16.1 窗口截断环）；护栏 64 防损坏流，
          击穿后武装保持放行至 EOF 冲刷（Push(nullptr) 确定性全量排空）兜底
  evidence: 酷刑流 bf16（bframes=16 b-adapt=0 金字塔，包序实测重排深度 17）
          3/3 干净；原挂死流 4/4 干净；env=0 消融挂死复现（承重）；
          护栏 16+酷刑流 22.7s 干净（EOF 兜底实证）；金标 28/28×2、压测
          9/9、TRT e2e/decode-only 同带；wheel 8e6f17a5 验证同源
  caveat: 酷刑流首转用错参数名（bf=16 被 x264 静默忽略只排 3 连 B）——
          正确键是 bframes；测量重排深度用包序逆序游程，别信 has_b_frames
  next: 已发布 v0.8.3（tag + cp39–cp314 + gpu.zip；CI cp313 抽验同绿）
  fork_commit: b67f9eb
# ── 2026-09-12 续：稳定性硬化轮（§18）──────────────────────────────────────
- metric: hybrid_release_gate
  question: 死锁/崩溃类时序敏感 bug 如何系统守住（§10.2/§16 两轮手工验证的固化）？
  result: tools/_probe_release_gate.py 六步 19 项一键门禁（金标/快慢双路径/
          压测/损坏码流/消融判别），dev dll 全绿 528s；损坏流现场生成
          （faststart+截断专打 demux 中途 EOF）
  evidence: 全门禁 19/19；ablation 步 KICK_BURST=0 挂死如预期（判别=机制承重）
  next: 发布前必跑（对 wheel 或待发 dll）；新视频/引擎分段语义变更需同步
          段数期望表
- metric: corrupt_stream_hardening
  question: 门禁首轮 corrupt 4/6 失败——截断流的崩溃/挂死根因？
  result: 双重 CHECK 致命化（fork d6028fe 修复）：filter Push 对 buffersrc
          拒收（截断尾帧 -12）CHECK abort=0xC0000409 崩溃；Pop EAGAIN 同型；
          异常路径下 worker 中断→析构 join 卡死=假挂起。改丢弃+早退后
          NextFrameImpl 既有 EOF 代偿（FetchCachedFrame 补位）接管——
          三 ctx 截断全优雅完成 14831/14831，ASAN 零报告
  caveat: 纯 NVDEC 路径本就幸免（无 ffmpeg filter 链）；观测上的"挂死"
          多数实为关闭路径 join 卡死，取证先看 CPU 占用（0%=阻塞非自旋）
  next: 已修，随 0.8.4 发布（v0.8.3 wheel 不含）
  fork_commit: d6028fe
- metric: stall_forensics_default
  question: pop-stall 取证能否默认开（不再要求先开 STATS 复现）？
  result: 时间判别器（3s 无进展首报 + 指数退避至 60s，进展即清零）；
          计数判别器证伪——健康供帧间隙毫秒级即凑满连败（误报 8 行/跑）。
          挂死 70s 打 14 行、健康双路径零误报（fork 32241f5）
  next: 已落地；0xC0000409 与 0% CPU 阻塞是两类故障的快速指纹
  fork_commit: 32241f5
# ── 2026-09-12 续：性能剩余项三笔关账（§19）────────────────────────────────
- metric: hybrid_perf_accounts_closed
  question: §13.4 遗留的供给侧三笔（克隆开销/上载天花板/优先级隔离）
  result: 全部关账。①克隆：clones 2880-3840/片=护栏 64 顶格（债务快照含
          积压，退化为 burst-64 语义）但 e2e 带内零回退——不动；②上载
          天花板：h264lg 全上载臂 −4.8%（~21µs/帧边际，顶 ≈2300-3000fps），
          仅 decode-only 快消费显形，引擎 h264→cpu 已绕行——不动；③优先级
          隔离：av1 宿主成对同向 −3~−6%，其余符号混乱——负面，补丁回退
  caveat: av1 的 FORCE_SIDE=cpu 在非 IDR 型下退化为全 GPU（机制设计），
          勿当上载臂读数
  next: 供给侧唯一有效杠杆仍是线程档位（§14）；剩余性能空间已穷尽，
        后续转向准确项
# ── 2026-09-17 续：ab 归因显著性与冷启动判定标定（§8）────────────────────
- metric: ab_gate_calibration            # bench ab 的热/冷带标定（子进程协议）
  hot_gpu_6pairs: {mean: +0.032, sd: 0.277, se: 0.113, p95abs: 0.145, hard: 0.40}
  hot_cpu_6pairs: {mean: +0.032, sd: 1.399, se: 0.571, p95abs: 1.738, hard: 1.75}
  cold_gpu_6pairs: {mean: -0.662, sd: 2.231, se: 0.911, p95abs: 2.864, hard_cold: 3.40}
  cold_cpu_6pairs: {mean: -1.332, sd: 2.114, se: 0.863, p95abs: 3.274, hard_cold: 3.95}
  note: 8 对历史值（第一段轮）GPU hot sd 0.254/1.45、CPU hot sd 1.22/1.75 级；
        冷带含引擎构建+时钟爬坡，幅值为热带 4~8 倍。换机器/窗口须重标。
- metric: w5_attribution_ground_truth    # 归因显著性的真值对照（C-50 机制）
  case: DECODE_THREADS=10 vs 32（h264-cpu，6 对，修复配对塌缩 bug 后）
  wall_verdict: 不可判定（-1.32%，SE 1.44%，符号 3/3）
  cold_verdict: B 显著慢 +5.78%（符号 6/6，限 4.0）
  attribution: decode.batch -80.52%±0.26（0/6 符号）**；q_get_wait -93.2%**；
               consume_feed +156%**；preprocess +76%**；infer +43%**；
               merge_pair +57%**——与 C-50 的 GIL 争用转移机制逐项吻合
  note: wall 判不了→冷判定抓冷启退化→归因给机制，三层口径互补的样例
# ── 2026-09-17 四段：分辨率压档与"不可分辨"结论复查（§9）────────────────
- metric: pi15_floor_evidence           # 地板 0.30→0.20 的证据链
  limit_calc_pct_by_round: [0.291, 0.209, 0.274, 0.198]   # |均值|+3SE 四轮
  n50_calibration: {mean: +0.006, sd: 0.451, se: 0.064, p95abs: 0.867, maxabs: 1.953}
  new_floor_pct: 0.20
  note: 低于 0.30 的建议值仅 n≥50 标定出现；PI15_LIMITS 0.30 为默认门禁限（n=25 匹配）
- metric: c47_defer_recheck             # C-47 复查（新遥测工具，8 对，full 档）
  wall: 不可判定（+0.167%±0.144%，符号 5/3，限 0.40）
  cold: 不可判定（-0.31%±0.93%）
  subphase_sync: {trt_enq: 0.244, trt_reduce_wait: 0.126, total: 0.370}
  subphase_defer: {trt_submit: 0.282, trt_collect_wait: 0.085, total: 0.367}
  verdict: C-47 维持——等待挪出 -32% 但 submit +15% 吃回，净持平；解码绑定吸收
- metric: c10_stream_recheck            # C-10 复查（8 对）
  wall: 不可判定（-0.094%±0.094%）；冷不可判定
  attribution: decode.stream_analyze +9.3%（7/8 **，基数 9.1ms/全片）
  verdict: C-10 维持默认关——零收益结论成立且开销首次被 span 定位
- metric: priority_meta_experiment      # §19 优先级隔离的判据层复查
  aa_with_above_normal_6pairs: {hot_sd: 0.530, cold_sd: 2.386, mean: -0.242}
  baseline_no_priority: {hot_sd_6p: 0.277, hot_sd_8p: 0.254}
  verdict: ABOVE_NORMAL 不收窄噪声带（无收益证据）——不为门禁采用，维持 opt-in
# ── 2026-09-17 五段：C-34 翻案（批量互补配对）──────────────────────────
- metric: pool_pairing_recheck          # C-34 复查 → 翻案（三臂，4 轮）
  material: racelog 三码族全片（7761/23970/23970 帧，roi 106×32，stride=1）
  arms_s: {pair: 27.0, nv2_all_nvdec_2w: 34.0, nv1_true_sequential: 34.2}
  gain_pct: {vs_nv2: -20.6, vs_nv1: -21.0}
  per_item_s: {h264_cpu: 2.51, h264_nvdec: 7.95, hevc_nvdec_solo: 11.7,
               av1_nvdec_solo: 14.1, hevc_2w: 24.0, av1_2w: 26.5}
  attribution: 收益主体=编码感知派工（h264→cpu）；并发零贡献（nv1≈nv2，
               双 NVDEC 各退化 ~2.1× = C-01 复证）；pair 27.0≈makespan 地板
  correctness: 三臂段数+唯一文本集逐位一致（4/4 轮）
  why_flipped: 旧判（2026-09-10）前提 OCR-GPU-bound 已被 C-48（OCR 2.1×）
               +C-49（decode-bound）颠覆
  probe_fixed: seq 臂名不副实（实为 2w 并发）→ 三臂制；新增 >10% 偏离中位
               的瞬态剔除（首跑 2 次 pair=34s 环境瞬态曾拖垮均值至 -2.2%）
# ── 2026-09-17 六段：fork 遥测穿透 + hybrid 引擎级重测（§11）────────────
- metric: fork_telemetry_passthrough      # report v6 'hybrid' 段（一次 extract 全拿）
  fork_side: HybridStatsProbe() 线协议（计数器全量+HOL log2 直方图×24+
            DECORD_HYBRID_TRACE=1 份额轨迹 1024 点）；五处改动见 log §11.1
  engine_side: 两后端 close 前取 → report v6 'hybrid' 键（缺席≠空值）
  smoke: 33 键（frames/chunks/kicks/busy µs/HOL ev+hist/rates/cache_peak）
  rebuild_notes: SDK 重建（ffmpeg-9.0 树内产物组装）/avdevice 空 def 占位/
            -DUSE_CUDA=ON 零 Toolkit（导入表无 nvcuvid 属正常，验证看 .obj）
- metric: hybrid_e2e_remeasure           # hybrid vs **目标码最快纯臂**（新协议；用户纠正口径）
  caliber_rule: hybrid 对照臂 = 该编码最快纯解码臂（h264→cpu ~2000fps；hevc/av1→nvdec）
  h264_w3000_vs_cpu: {cpu_s: 1.102, hybrid_s: 1.688, delta_pct: +53.6, n: 8,
               signs: 0/8 慢, se_pct: 1.7, cold_pct: +41.4}   # hybrid 大败
  h264_w3000_vs_nvdec_wrongcaliber: {delta_pct: -46.5}   # 错基线（nvdec 非最快臂），仅留档
  hevc_full23970: {hybrid_s: 9.21, delta_pct: -23.0, n: 6, signs: 6/6,
                   se_pct: 0.43, cold_pct: -21.4}
  av1_full23970: {hybrid_s: 9.53, delta_pct: -33.8, n: 6, signs: 6/6,
                  se_pct: 0.55, cold_pct: -34.2}
  window_trap: hevc 在 window=3000（C-40 交叉点内侧）hybrid 反而 +94% 慢——
               hybrid 评估必须越过目标码的交叉点窗口
  fork_readings: h264 CPU 份额 72→76%；hevc GPU 75% 帧+HOL 757ep；
               av1 CPU 份额 25→33% 爬升（泵适应轨迹首次可见）
  caliber_note: C-46 的"−6.29%/−4.78%"经核对是 fork 新旧机制 A/B，非
               hybrid vs nvdec e2e——口径混用自此澄清
- metric: h264_hybrid_engine_reversal_decomp  # §11.7：解码单跑赢→引擎反转的成分分解
  probe_today: {cpu_single_fpsb: 3613, gpu_single: 991, hybrid: 4213,
                hybrid_cpu_arm_busy: 3214, hol_g_s: 0.80}   # 定稿协议全复现
  engine: {cpu_decode_fps: 3363, hybrid_w3000_wall: 1.636, hybrid_cpu_arm_busy: 1452,
           cache_peak_mb: 15, tight_loop_cache_mb: 127}
  window: {w3000_vs_cpu: +53.6, full_vs_cpu_32T: +11.4, full_vs_cpu_10T: +25.8}
  thread_sweep_w3000: {32T: 1.636, 16T: 1.459, 10T: 1.416}
  verdict: 主因=供给-调度耦合（消费者批量拉取使 demux 半饿、泵欠供+暖机）；
          次因=线程超额（短窗 −13.5% 降线程好，长窗反向要线程）；HOL 小
  fix_directions: 供给解耦（预拉/深 prefetch）或暖机加速（速率先验）——未实施
# ── 2026-09-17 七段：hybrid 越窗读问题范围调查（§11.8，不动代码）────────
- metric: hybrid_window_overread_scan     # assigned帧/窗口帧（穿透版 stats）
  h264_test5_full7761: {w500: {assigned: 6279, ratio: 12.56, busy_s: 0.70, need_s: 0.15},
                        w1000: {assigned: 7178, ratio: 7.18, busy_s: 1.00},
                        w3000: {assigned: 7761, ratio: 2.59, busy_s: 2.34, need_s: 0.88},
                        full: {assigned: 7761, ratio: 1.00}}
  hevc_test6_full23970: {w1000: {assigned: 17280, ratio: 17.28},
                         w3000: {assigned: 23970, ratio: 7.99, busy_s: 2.87},
                         full: {ratio: 1.00, busy_s: 10.27}}
  cpu_nvdec_control: 线性（nvdec w3000 3.08s≈理论3.03；cpu 偏差为固定启动
                     成本非窗口比例）——大规模越窗为 hybrid 独有
  hevc_engine_crossover: nvdec/hybrid 同窗（含OCR）w1000 0.55/0.91、
                         w3000 1.51/2.20、full 11.68/8.75 → 交叉点在
                         (3000, 23970)，与 C-40 的 1500~3000 矛盾
  mechanism: hybrid demux 由消费驱动、包缓存预算（512MB）≥ 素材体积 →
            一次性读至 EOF；ROI 模式银行帽 4096/12352 帧从未成为瓶颈。
            正确性不受影响（逐位一致/golden 干净），代价=纯解码浪费
            （越窗比×臂速）。
  timeline: v0.8.0(09-06) hybrid 诞生即带深 prefetch（钳 384，越窗已存在
            但有界）；v0.8.2 NeedsPackets 雏形；v0.8.3(现役 wheel) 包缓存
            预算+ROI 银行重算 → 越窗在实践中无上界。引擎 hybrid-native
            采用自 09-11 前后——全部 hybrid 短窗测量史都在污染面上。
  tainted: C-40 交叉点与短窗劣势数字、C-41 短窗缺口（含税读数）；
          full-file 协议结论（C-46/C-53 hevc/av1、C-52）与 golden 干净
- metric: cpu_nvdec_overread_audit        # §11.8 续：cpu/nvdec 越界量化
  right_edge: prefetch 基础深度恒 8 包（DECORD_PREFETCH_DEPTH_BASE，
              video_reader.cc:84）；EOF 锚定窗口无额外代价
  left_edge:  keyframe seek-back 固有（解码必须自关键帧起）——racelog
              test5 GOP max 299/mean 288 帧 ≈ 0.09s@cpu；引擎
              seek_accurate 重锚循环上界 4096 Pop（≈2 GOP 最坏）
  position_test: cpu [0,3000)=1.736/[3000,6000)=1.640/[4761,7761)=1.752；
              nvdec 3.130/3.059/3.310——位置无关，无窗口比例分量
  evidence_note: （已被下条包级直证推翻）间接口径漏掉了 retry 循环推包
- metric: overread_definitive_matrix       # 包级直证（DecodeStats 打桩）
  instrument: VideoReader::DecodeStats() → pkts_pushed/frames_popped（穿透
              版+本轮打桩；注意 EOF 后 PushNext 空推也计数，数字略虚增）
  matrix_overread_pkts:
    cpu:   {w64: 2882, w500: 7390, w3000: 7423, full: 7420}
    nvdec: {w64: 30, w500: 30, w3000: 30, full: 46}
    hybrid: {w64: 3147, w500: 7262, w3000: 4762, full: 1}
    cpu_full_note: cpu 全片总推 15181 = 全文件双读
  mechanism: retry 循环 NeedsPackets 对基础 CPU 解码器恒真 → 消费者自旋
            每次推一包、无上界，CPU Push 入深队列不阻塞 → demux 读完整
            文件；NVDEC Push 受 20 surface 硬背压 → demux 节流跟随消费
  reversal: 推翻"cpu/nvdec 有界"的间接口径——cpu 与 hybrid 同病（demux
          全文件 + 越窗解码并行烧核，wall 被并行吸收故此前未察觉；
          decode 相位 cores_avg 16.42 的大头即越窗解码）；nvdec 独净
  waste_form: cpu 越窗解码在 32 线程里并行烧掉（不进 wall，进核秒/功耗/
            与消费 GIL 争用）——C-50/C-52 的 GIL 争用放大器
- metric: overread_fix_verification        # §11.11 cpu NeedsPackets 如实化验证
  fix: FFMPEGThreadedDecoder::NeedsPackets 覆盖（PendingDepth+QueueDepth < 256，
       DECORD_INFLIGHT_CAP 可调）+ retry 循环幻影计数修复
  post_fix_overread: {cpu: {w64: 291, w500: 291, w3000: 291, full: 0},
                      nvdec: 不变(30), hybrid: 不变(自有门控)}
  wall_regression: bare 同DLL交替 cap256 中位 1.518 vs 旧行为 1.534（-1.0%
                   且散布更紧）；引擎 ab +1.41% 符号 5/1 不可判定——无回归
  correctness: 段数 1090、金标 28/28、纪律审计 0
  attribution_correction: §11.10"cores_avg 大头=越窗解码"被修复实验证伪
    （关掉后 16.42→16.45 不变）——越窗包大多死在 pkt_queue（close 丢弃，
    ROI 解码太便宜），真实代价=demux I/O+pkt_queue 内存（160MB→6MB）；
    decode 相位 16 核构成另立开放问题
# ── 2026-09-17 八段：hybrid 硬窗界 + 死锁修复 + C-40 改判（§11.12-13）──
- metric: hybrid_hard_window            # SetDecodeWindow 全链 + 排空修复
  impl: NeedsPackets 窗界门（assigned≥窗仅放行边界 GOP）+ PumpFeed 派工
        硬停 + window_closed 触发排空标记（窗外 kick 丢弃不兜底重锚）
  deadlock: 双重——①排空条件只认 EOF 游标条件，窗缘永不满足 → worker
        join 死锁；②NVDEC 输入断流 → 7 帧扣留 DPB（DiagDepths cud=0/7/0）
        → 显示回调停 → 消费者饿死。修复=窗界关断即发排空
  verify_w3000: {assigned: 3289(修前7761), wall: 1.524-1.540(修前1.58-1.68),
                 hol_g: 0.03(修前0.22), cpu_arm_busy_fps: 2761(修前1218),
                 segments: 1090}
  full_window_matrix: {w64: {assigned: 299, wall: 0.199}, w500: {assigned: 598},
                       w1000: {assigned: 1196}, full: {assigned: 7761, wall: 2.713}}
- metric: c40_remeasure_after_fix       # C-40 复测（硬窗界后，干净口径）
  hevc_w3000: {hybrid_vs_nvdec: +35.2%, signs: 6/0 慢}   # 交叉点 >3000
  hevc_full: {hybrid_vs_nvdec: -23.6%, signs: 4/4 快}
  av1_w3000: {hybrid_vs_nvdec: -16.3%, signs: 4/4 快}    # 交叉点 <3000
  av1_full: {hybrid_vs_nvdec: -31.2%, signs: 4/4 快}
  verdict: C-40 改判——hevc 交叉点 (3000,23970) 非 1500~3000；av1 确认；
          h264 不变（选 cpu，C-53）
  note: 全片数字修复前后稳定（hevc -23.0→-23.6 / av1 -33.8→-31.2）——
        硬窗界全片零影响（不设界）
